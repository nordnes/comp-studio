# Rubric grade 001 — Wave 0 scoped grader

- **Panel:** Wave 0 boundary check (single independent fresh-context grader, adversarial)
- **Date:** 2026-06-10
- **Branch:** `claude/frosty-pasteur-8cf1db`
- **Frosty tip at grading:** `7ed0423fec49ff1402d06a11406b4ed76d6508c0` (== `origin/claude/frosty-pasteur-8cf1db`, verified after `git fetch`)
- **Scope:** R1.3 · R8.3 (Wave-0 slice) · R2.1 · R2.2 (current-state form) · rubric-versioning precondition for R8.4. Not a full panel.

---

## R1.3 — PR #68 merged, COM-139 Done, corpus position is Plan v9 — **PASS**

- `gh pr view 68 --json state,mergedAt,title,headRefName` →
  `{"headRefName":"robinandre/com-139-coc-correction","mergedAt":"2026-06-10T10:48:41Z","state":"MERGED","title":"fix(COM-139): correct the CoC-acceleration legal line to the Plan v9 position — HOLD for GC sign-off"}`
- Linear `get_issue(COM-139)` → `"status":"Done","statusType":"completed"`, completedAt `2026-06-10T10:49:46Z` (state history: Backlog → In Progress 2026-06-10T02:16 → Done 2026-06-10T10:49).
- New wording present in shipped source (`scaffold/src`):
  - `scaffold/src/views/Proposition.vue:291-293` — "No automatic or discretionary change-of-control acceleration under the plan (Plan rules v9); on a change of control the acquirer may roll over awards, and vested options remain exercisable per the plan rules." (exact spec v2 Part 10 #6 position)
  - `scaffold/src/components/VestingTimeline.vue:149-150` — close variant: "No change-of-control acceleration under the plan (v9); an acquirer may roll over awards."
- Stale reference wording GONE from shipped source: the reference's stale line is `reference/advisor-comp-studio.tsx:1100` "Change-of-control acceleration is at Board discretion (not contractual)." — `grep -rni "discretion" scaffold/src/` returns exactly ONE hit, and it is the NEW wording (Proposition.vue:291 "No automatic or discretionary…"). `grep -rni "acceleration" scaffold/src/` returns only the v9-position lines plus the COM-139 removal comment at `scaffold/src/views/Configure.vue:460-462` (the reference's CoC-acceleration control was deleted, cocAccelPct left inert).
- Render-path sweep: Proposition screen+print share the template corpus (new wording present); `propText()` clipboard (`Proposition.vue:30-59`) contains NO CoC sentence in either direction — stale line absent there too. Minor observation (not a fail under this criterion's text): the clipboard text omits the legal corpus paragraph entirely rather than carrying the v9 sentence.

## R8.3 (Wave-0 slice) — clean tip, OBJECTIVES_AND_FLOWS.md resolved, churn files match origin — **FAIL**

- OBJECTIVES_AND_FLOWS.md resolved: PASS-component. `git ls-files --error-unmatch OBJECTIVES_AND_FLOWS.md` → "did not match any file(s) known to git"; `ls OBJECTIVES_AND_FLOWS.md` → "No such file or directory". Absent from worktree AND untracked.
- `git status --porcelain` → ` M scaffold/components.d.ts` — the working tree is NOT clean, and this is not the harness-noise line (the only sanctioned noise is the `engine/engine.ts: Operation not permitted` read-denial). File mtime 2026-06-10 11:50; the grader ran nothing that regenerates it (only git/grep/sed/node test commands).
- Churn files vs origin: `git diff --stat origin/claude/frosty-pasteur-8cf1db HEAD -- scaffold/auto-imports.d.ts scaffold/components.d.ts scaffold/package-lock.json` → empty (committed state matches origin), BUT the WORKING TREE copy of `scaffold/components.d.ts` differs from origin by 11 insertions / 2 deletions (`git diff origin/... -- scaffold/components.d.ts`): adds CommandPalette, EmptyState, ExitSlider, PackageEditor, Panel, RosterIdentity/RosterRow/RosterTable/TierBadge, ScenarioTable, Term; removes PotentialStrip, StageBadge. This also implies the COMMITTED components.d.ts is stale relative to the actual `scaffold/src/components/` contents.
- Verdict: FAIL on the "git status clean" clause. Remedy is mechanical: commit the regenerated `scaffold/components.d.ts` (or restore it if the regeneration is premature) so worktree == tip == origin.

## R2.1 — v1 suites 22/22 in both locations — **PASS**

- `node engine/engine.test.mjs` → tail: `22 passed, 0 failed.`
- `node scaffold/engine.test.mjs` → tail: `22 passed, 0 failed.`
- Both run sandboxed, exit clean, suites unmodified at tip.

## R2.2 (current-state form) — v2 suite 0 failed, pendings stage-appropriate — **PASS (stage-appropriate: pending bindings remain, 0 failed)**

- `node engine/engine.v2.test.mjs` → final line: `37 passed, 0 failed, 6 pending(v2).`
- Pending count: **6** `PENDING(v2)` lines, exactly the six T5 bindings (walkScenario FD parity · vestedFracRTA · distributableFrac 24-mo gate · valueToQuantity · computeGrant per-grant strike · modelDeparture Bad Leaver) — expected at the Wave-0 boundary, engine wave not started.
- Note: full R2.2 (zero PENDING lines) remains UNMET by design at this stage; this grade is the current-state form prescribed for Wave 0.

## Rubric versioning (R8.4 precondition) — rubric files tracked on frosty — **PASS**

- `git ls-files | grep -E 'RUBRIC_V2_FINISH|ULTRACODE_V2_FINISH'` → `RUBRIC_V2_FINISH.md` and `ULTRACODE_V2_FINISH.md` both tracked. Rubric history is auditable via `git log -p -- RUBRIC_V2_FINISH.md`.

---

**Summary: 4 PASS / 1 FAIL.** The single FAIL is R8.3's clean-tip clause: working-tree dirt at `scaffold/components.d.ts` (regenerated component declarations, 11+/2−, vs origin), while the committed state matches origin and OBJECTIVES_AND_FLOWS.md is fully resolved.
