# Rubric grade 002 — Wave 1 mid-flight scoped grader

- **Panel:** Wave 1 mid-flight check after COM-142/143/144 (PRs #71–#75) — single independent fresh-context grader, adversarial
- **Date:** 2026-06-10
- **Branch:** `claude/frosty-pasteur-8cf1db`
- **Frosty tip at grading:** `d415b2f2d6a91de6304b0ccde7aeb267d35585fa`
- **Scope:** R2.1 · R2.2 (stage-appropriate form) · R2.3 · R2.4 (so-far form) · R2.5 · R5.1 (so-far form) · R5.3 · R3.2 (live). Not a full panel. Engine wave mid-flight: v2 pendings for COM-145/150/153 are expected per the orchestrator scope.
- **Working tree at grading:** clean (`git status --porcelain` shows only the sanctioned harness read-denial noise on `engine/engine.ts`).

## Dist-freshness precondition — SATISFIED

- `cd scaffold && npm run build` → exit 0, `✓ built in 2.89s`, emits `dist/assets/index-CKXvwLcE.js` (965.54 kB) + `dist/assets/index-sScgB_s_.css`.
- `fetch('http://[::1]:4173/')` → 200, HTML references `assets/index-CKXvwLcE.js` and `assets/index-sScgB_s_.css` — exact hash match with the fresh build. The preview serves the just-built bundle; R3.2 was graded against it.

---

## R2.1 — both 22-vector suites green — **PASS**

- `node engine/engine.test.mjs` → `22 passed, 0 failed.`
- `node scaffold/engine.test.mjs` → `22 passed, 0 failed.`
- Immutability (RFC §7 context, checked adversarially): `git diff --stat 3bf3e20..HEAD -- engine/engine.test.mjs scaffold/engine.test.mjs` → empty. Both v1 suites are byte-identical to baseline.

## R2.2 (stage-appropriate) — v2 suite 0 failed, 4 expected pendings — **PASS**

- `node engine/engine.v2.test.mjs` → `98 passed, 0 failed, 4 pending(v2).`
- The four PENDING(v2) lines, verbatim, match the expected mid-wave set exactly (COM-145/150/153 not yet landed):
  1. `vestedFracRTA(m) equals the T3 reference across months 0–60`
  2. `distributableFrac(grant, m, serviceMonths) applies the 24-month qualifying gate`
  3. `valueToQuantity(grant, scenario) equals the T4 reference, restated per scenario`
  4. `modelDeparture(): Bad Leaver → vested+unvested options lapse, tokens forfeit (COM-153)`
- No unexpected pendings; the COM-142/143/144 T5/T6/T7 bindings run LIVE against `scaffold/src/engine.ts` (suite line 99: `const ENG = await import(new URL('../scaffold/src/engine.ts', import.meta.url).href)`).

## R2.3 — A.3 anchors reconcile in the LIVE engine — **PASS**

One-off script (`/tmp/claude/r23-assert.mjs`) importing `scaffold/src/engine.ts` directly under node v24.16.0, calling `walkScenario(DEFAULT().plan, 'base')`:

```
PASS bridge N:     actual=57217.23947368421  expected=57217  ±1     (Δ=0.239474)
PASS seriesA N:    actual=75359.29101412067  expected=75359  ±1     (Δ=0.291014)
PASS seriesB N:    actual=89379.62422605009  expected=89380  ±1     (Δ=-0.375774)
PASS seriesC N:    actual=118707.31342522278 expected=118707 ±1     (Δ=0.313425)
PASS bridge price: actual=1572.9525022155865 expected=1572.95 ±0.01 (Δ=0.002502)
ALL ANCHORS RECONCILE
```

## R2.4 (so-far form) — SCHEMA untouched — **PASS**

- `git log -G'SCHEMA = ' --oneline 3bf3e20.. -- scaffold/src/engine.ts` → EMPTY (exit 0). No commit since baseline touched the SCHEMA line.
- `grep -c 'SCHEMA = 5' scaffold/src/engine.ts` → `1` (line 113: `export const SCHEMA = 5;`).
- Corroborating: engine.ts:58 comment — "v2 (COM-142): constitutional baseline — additive, SCHEMA stays 5; reconcile() defaults them." All v2 state additions land via reconcile seeding, not a version bump. COM-171 remains the only sanctioned SCHEMA toucher and has not run.

## R2.5 — zero unsanctioned money math in views — **PASS**

Sweep = full grep of `scaffold/src/views` + `scaffold/src/components` for arithmetic on value/price/percent-class identifiers, plus a line-by-line read of every diff hunk since baseline (`git diff 3bf3e20..HEAD` — files changed: Configure.vue, Advisors.vue, Proposition.vue, VestingTimeline.vue, store.ts).

**New surfaces (closest read):**
- `Configure.vue` constitutional section: ALL money computation delegated to engine exports — `poolGuardrail(S.S.plan)` (guardrail verdict), `poolSharesExact(S.S.plan, 0.15)` (the 15%-cell recompute callout), `tokenPoolHeadroom(tp)` (per-pool headroom). The view renders `guard.level/msg/poolShares/cap` and formats (`fShares`, `fPct`, `fNum`). The only operators in added lines are string templates and equality comparisons (`advisorPoolShares === 8523`). No violation.
- `Advisors.vue` grant rows: every row figure comes from `computeGrant(g, plan, scenKey)` output — `r.strikePps`, `r.fmvPps`, `r.value`, `r.underwater` rendered verbatim with `.toFixed`/`fUSD` formatting only. Row derivation via `effectiveGrants(...)`. No view-side pricing. No violation.
- `store.ts` grant CRUD (COM-144): `materialiseGrants` copies `effectiveGrants()` output; `addGrant` pushes a zeroed grant (`quantity: 0` / `valueUSD: 0`) with the round id — no price/value computed in the store. No violation.
- `Proposition.vue` / `VestingTimeline.vue` diffs: legal-copy changes only (the Δ4/v9 CoC wording). No arithmetic touched.

**Pre-existing surfaces (operator grep, each candidate dispositioned):**
- `components/UpsideCurve.vue:27` `Math.max(0, props.c.eqPct * (sb.retention * V - sb.strikeBasis))` — exact mirror of `engine.ts:551` `netEqAt = (pct, V) => Math.max(0, pct * (retention * V - strikeBasis))` over exported fields (`c.scen/base` from `computeAdvisor`). Sanctioned.
- `components/PackageEditor.vue:219` `S.plan.baseGrant.tokenPct * t.mult` — mirror of `engine.ts:524` `baseTk = bg.tokenPct * tierMult`. Sanctioned.
- `views/Overview.vue:32`, `views/Board.vue:226-229`, `views/Compare.vue:27,40` — `reduce((s, r) => s + r.c.baseEq, 0)` etc.: sums of engine-exported per-advisor outputs (`computeAdvisor`/`computeBoard` rows), no new money semantics. Sanctioned.
- `views/Proposition.vue:46`, `components/PackageEditor.vue:339` — `pct * 100` display conversion. Display rounding, sanctioned.
- `views/Board.vue:168-170`, `components/GrowthWaterfall.vue:90-91`, `components/MixBreakdown.vue:32`, `components/VestingTimeline.vue:28` (pre-baseline), `components/UpsideCurve.vue:19,37,44,66` — chart axis/geometry/sampling over engine-exported values. Sanctioned.

No line computing value/price/percent from raw state was found. Zero violations cited.

## R5.1 (so-far form) — every NEW engine export has vectors in the v2 suite — **FAIL**

Exports added since baseline (`git diff 3bf3e20..HEAD -- scaffold/src/engine.ts | grep '^+export'` → 25 names; no exports removed; no non-line-start export additions). By-name grep of `engine/engine.v2.test.mjs` (`grep -c '\b<name>\b'`):

| Export | Suite hits | Disposition |
|---|---|---|
| computeGrant | 13 | vectored |
| walkComposed | 7 | vectored |
| poolGuardrail | 6 | vectored |
| planWithSet | 5 | vectored |
| effectiveGrants | 5 | vectored |
| METHOD_NOTES | 4 | vectored |
| makeScenarioSet | 3 | vectored |
| poolSharesExact | 2 | vectored |
| tokenPoolHeadroom | 1 | vectored |
| POOL_PRESETS | 1 | vectored |
| FD_COMPOSITION | 1 | vectored |
| **GRANT_LIFECYCLES** | **0** | runtime const — MISS by name |
| **DOC_STATUSES** | **0** | runtime const — MISS by name |
| **ENTITY** | **0** | runtime const — MISS, no coverage in any form |
| **CONSTITUTION_DEFAULT** | **0** | runtime const — MISS by name |
| **TOKEN_POOLS_DEFAULT** | **0** | runtime const — MISS by name |
| **setList** | **0** | runtime fn — MISS by name |
| **currentRoundStep** | **0** | runtime fn — MISS by name |
| Instrument / GrantLifecycle / DocStatus / Grant / ScenarioSet / Constitution / TokenPool | 0/0/0/1*/2*/0/0 | type-only — erased at runtime; a `.mjs` suite cannot reference them (*comment hits only) |

The criterion's pinned mechanical check (grep the suite for each export) fails for **7 runtime exports**. Mitigating context, recorded for the builder (does not flip the verdict):
- `CONSTITUTION_DEFAULT` / `TOKEN_POOLS_DEFAULT` values ARE asserted by value (suite lines 131–146, 157–158: authorised 50,000 · issued 37,550 · poolAvailable 12,450 · 4 pools · advisors headroom 1.82552%) — but not via the export.
- `GRANT_LIFECYCLES` / `DOC_STATUSES` semantics are exercised through the lifecycle/docStatus heal vectors (suite lines 372–419) — but the exported arrays themselves are never asserted (e.g. a membership/ordering vector).
- `setList` is exercised only indirectly via `planWithSet` (engine.ts:390); `currentRoundStep` only indirectly via `computeGrant`/fold stage-mapping vectors (engine.ts:499, 593).
- `ENTITY` (legal identity constant, rendered on Configure.vue:427-428) has **no coverage in any form**.

Remedy: add by-name vectors for the 7 runtime exports (cheap — most are one-line asserts), **or** tighten R5.1 in a `docs:` PR to scope it to runtime exports and define what counts as a vector for re-exported constants. Note for the rubric owner: type-only exports are mechanically uncheckable from a `.mjs` suite — per the protocol ("a criterion that cannot be mechanically checked is a rubric bug"), R5.1 needs a one-line clarification regardless.

## R5.3 — mirror convention on the three highest engine-touching merged PRs — **PASS**

`env -u GH_TOKEN gh pr view N --json state,files` for #71–#75 (all five MERGED):
- #75 (COM-144 UI): files = `scaffold/src/store.ts`, `scaffold/src/views/Advisors.vue` — does NOT touch engine.ts (correctly excluded from the sample).
- **#74** (COM-144 engine): files = `engine/engine.v2.test.mjs`, `scaffold/src/engine.ts` — suite touched ✓
- **#73** (COM-143 engine): files = `engine/engine.v2.test.mjs`, `scaffold/src/engine.ts` — suite touched ✓
- #72 (COM-142 UI): files = `scaffold/src/views/Configure.vue` — does NOT touch engine.ts (correctly excluded).
- **#71** (COM-142 engine): files = `engine/engine.v2.test.mjs`, `scaffold/src/engine.ts` — suite touched ✓

The three highest-numbered merged PRs touching `scaffold/src/engine.ts` are exactly #74, #73, #71, and every one of them also modified `engine/engine.v2.test.mjs` in the same PR. 3/3 — no discrepancy, no expansion triggered.

## R3.2 (live) — second grant at a later round → per-grant strikes differ — **PASS**

Playwright probe (`/tmp/claude/r32-probe.mjs`) against the fresh-built preview at `http://[::1]:4173/advisors`, chromium headless:

1. **localStorage snapshot taken** before any interaction → `{}` (no persisted state; the app was running on seeded defaults).
2. **Instruments tab** located via `getByRole('tab', { name: 'Instruments' })` → count 1, clicked.
3. **Rows before:** 2 derived (implicit-package) grant rows — `option @ Bridge, strike $1572.95, FMV $1572.95, count 858, $2.27M, granted` and `rta, strike —, FMV $0.060, count 90.0M, $5.40M, granted`.
4. **Add grant → Option grant** via the `Add grant` dropdown button → menu item clicked.
5. **Rows after:** 3 rows — the two above PLUS `option @ Series C, strike $4212.04, FMV $1572.95, count 0, $0, draft`. The new grant defaulted to the latest round (Series C) per the Part 5.3 top-up story, and its strike is the Series C round price.
6. **Assertions:** rows ≥ 2 → PASS (3 grant rows). Option strikes DIFFER → PASS (`$1572.95` vs `$4212.04`, distinct set size 2). Strike behaviour is engine-consistent: $4,212.04 = the Series C walk price (`walkScenario` byId.seriesC.price = 4212.0404…), FMV stays the current-stage (bridge) price $1,572.95 per the Cert v3 cl. 4.5(c) methodology — matching the live v2 vectors.
7. **State restored:** localStorage cleared and snapshot re-applied, page reloaded → post-reload localStorage exactly equals the pre-flow snapshot (`RESTORE exact match: PASS`). App left as found.
8. **Console:** zero error-level entries across the whole flow.

Screenshot evidence: `/tmp/claude/r32-after.png`.

---

## Scoped verdict summary

| Criterion | Verdict |
|---|---|
| R2.1 | PASS |
| R2.2 (stage-appropriate) | PASS (0 failed, 4 expected pendings) |
| R2.3 | PASS |
| R2.4 (so-far) | PASS |
| R2.5 | PASS |
| R5.1 (so-far) | **FAIL** (7 runtime exports with zero by-name vectors; ENTITY wholly uncovered) |
| R5.3 | PASS |
| R3.2 (live) | PASS |

7/8 PASS. The single FAIL (R5.1) is mechanical and cheaply remediable mid-wave; it does not indicate an engine-integrity problem (all behavioural coverage that matters for money math exists — the misses are by-name vector gaps plus one untested static constant).
