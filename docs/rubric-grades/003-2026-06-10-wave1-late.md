# Rubric grade 003 — Wave 1 late scoped grader

- **Panel:** Wave 1 late check after COM-150/151/153 (PRs #77–#80) — single independent fresh-context grader, adversarial
- **Date:** 2026-06-10
- **Branch:** `claude/frosty-pasteur-8cf1db`
- **Frosty tip at grading:** `081245f23569976cf4da13dc0d3422c753ba6b6e`
- **Scope:** R2.1 · R2.2 (FULL form) · R2.3 · R2.4 (so-far form) · R2.5 · R5.1 · R5.3 · R3.16-partial (live slice) · R4.7-slice (new copy since panel 002). Not a full panel. Engine wave: COM-152/146/154/171 remain; the full R3.16 toggles are COM-152/154 and were NOT graded here.
- **Working tree at grading:** `git status --porcelain` → empty (clean). The formerly-untracked `OBJECTIVES_AND_FLOWS.md` no longer exists in the tree.
- **"Since 002" baseline:** panel 002's frosty tip `d415b2f`. Delta: `scaffold/src/engine.ts` (+314), `Advisors.vue` (+32), `Configure.vue` (+32), `Proposition.vue` (+19/−20), `PackageEditor.vue` (+19).

## Dist-freshness precondition — SATISFIED

- `cd scaffold && npm run build` → exit 0, `✓ built in 2.98s`, emits `dist/assets/index-BVWx4TsT.js` (972.64 kB / gzip 287.41 kB) + `dist/assets/index-sScgB_s_.css`.
- `fetch('http://[::1]:4173/')` → HTML references `assets/index-BVWx4TsT.js` — exact hash match with the fresh build (asserted inside the playwright probe, first assertion). All live grading ran against this bundle.

---

## R2.1 — both 22-vector suites green — **PASS**

- `node engine/engine.test.mjs` → `22 passed, 0 failed.`
- `node scaffold/engine.test.mjs` → `22 passed, 0 failed.`
- Immutability (RFC §7 context, checked adversarially): `git diff --stat 3bf3e20..HEAD -- engine/engine.test.mjs scaffold/engine.test.mjs` → empty. Both v1 suites byte-identical to baseline.

## R2.2 (FULL form) — v2 suite 0 failed, ZERO pendings — **PASS**

- `node engine/engine.v2.test.mjs` → **`167 passed, 0 failed, 0 pending(v2).`**
- `node engine/engine.v2.test.mjs | grep -c 'PENDING(v2)'` → `0`. The four pendings open at panel 002 (vestedFracRTA · distributableFrac · valueToQuantity · modelDeparture) are gone — all T5 bindings run LIVE against the real engine (suite line 99: `const ENG = await import(new URL('../scaffold/src/engine.ts', import.meta.url).href)`).
- New tiers visible in output: T12 (COM-151 exercise windows/3.6 backstop) fully green, including the backstop-date vector `9th anniversary 2035-06-01 · last close 2036-05-31`.

## R2.3 — A.3 anchors reconcile in the LIVE engine — **PASS**

One-off script (`/tmp/claude/pw/grade003-anchors.mjs`) importing `scaffold/src/engine.ts` directly under node v24.16.0, calling `walkScenario(DEFAULT().plan, 'base')`:

```
PASS bridge N:              actual=57217.23947368421  expected=57217  ±1
PASS seriesA N:             actual=75359.29101412067  expected=75359  ±1
PASS seriesB N:             actual=89379.62422605009  expected=89380  ±1
PASS seriesC N:             actual=118707.31342522278 expected=118707 ±1
PASS bridge price (strike): actual=1572.9525022155865 expected=1572.95 ±0.01
R2.3: ALL ANCHORS RECONCILE
```

## R2.4 (so-far form) — SCHEMA untouched — **PASS**

- `git log -G'SCHEMA = ' --oneline 3bf3e20.. -- scaffold/src/engine.ts` → EMPTY (exit 0). No commit since baseline touched the SCHEMA line.
- `grep -c 'SCHEMA = 5' scaffold/src/engine.ts` → `1` (line 128: `export const SCHEMA = 5;`). COM-171 remains the only sanctioned SCHEMA toucher and has not run.

## R2.5 — zero unsanctioned money math in views — **PASS**

Closest read = full diff `d415b2f..HEAD` of the four surfaces changed since panel 002, plus an operator grep over them (panel 002's full-tree sweep stands for everything unchanged since `d415b2f`).

- **`Advisors.vue` (exercise mechanics block + backstop computed):** the new `backstop` computed filters option grants by lifecycle, sorts by `vestStartISO` (date/display selection, not money semantics), and delegates entirely to `exerciseCheck(opts[0], todayISO()).backstop` — the engine computes both dates. Rendering uses `fDate(backstop.anniversary9ISO)` / `fDate(backstop.lastCloseISO)` verbatim. The mechanics block renders the `EXERCISE_MECHANICS` and `FUNDING_ROUND_CARVEOUT` engine constants verbatim. Zero arithmetic in the added hunks. No violation.
- **`Proposition.vue` (fine-print expansion):** copy-only diff — the 3.6/4.5/7.4(a) sentences plus `{{ FUNDING_ROUND_CARVEOUT }}`. No arithmetic touched. No violation.
- **`PackageEditor.vue` (band quick-selects):** active-state via equality (`sel.annualValue === b.annualUSD`), selection via `setField('annualValue', b.annualUSD)` (raw assignment of a configured anchor), display via `fUSD(b.annualUSD)`. No computation. No violation.
- **`Configure.vue` (value bands):** `NumIn` bound to `b.annualUSD` through `setPath(['plan','valueBands',bi,'annualUSD'], v)` — raw editing of configured anchors; no derived money figures. No violation.
- Operator grep over the four files surfaced only pre-existing lines already dispositioned in panel 002 (the COM-84 target-outcome lerp mirror over engine per-scenario exports, `pct * 100` display conversions, `fNum(cc.baseEq * b.grantN)` engine-export product in propText — the engine-formula mirror sanctioned at 002).

Zero violations cited.

## R5.1 — every NEW engine runtime export has a by-name vector — **PASS**

`git diff 3bf3e20..HEAD -- scaffold/src/engine.ts | grep '^+export'` → 44 export lines = **34 runtime (value) exports** + 10 type-only exports (`Instrument`, `GrantLifecycle`, `DocStatus`, `Grant`, `ValueBand`, `ScenarioSet`, `Constitution`, `TokenPool`, `ExerciseWindow`, `LeaverType` — exempt per the R5.1 precision note: erased at runtime).

By-name grep of `engine/engine.v2.test.mjs` (`grep -c '\b<name>\b'`) — every one of the 34 ≥ 1 hit, and the suite references are real exercises via the `ENG.` namespace (the live-engine import), not import lines:

| hits | exports |
|---|---|
| 1 | dayBeforeISO · FD_COMPOSITION · POOL_PRESETS · tokenPoolHeadroom |
| 2 | addYearsISO · DOC_STATUSES · FUNDING_ROUND_CARVEOUT · GRANT_LIFECYCLES · PLAN_DISQUALIFICATION_WARNINGS · poolSharesExact · VALUE_BANDS_DEFAULT |
| 3 | BAD_LEAVER_LIMBS · classifyLeaver · CONSTITUTION_DEFAULT · currentRoundStep · ENTITY · makeScenarioSet · setList · TOKEN_POOLS_DEFAULT |
| 4 | EXERCISE_MECHANICS · fullMonthsBetween · METHOD_NOTES · valueToQuantity · vestedFracRTA |
| 5+ | effectiveGrants(5) · planWithSet(5) · vestedAtDate(5) · poolGuardrail(6) · vestedAtMonths(7) · walkComposed(7) · exerciseCheck(9) · distributableFrac(14) · computeGrant(21) · modelDeparture(22) |

Direct-exercise spot-checks on every 1-hit name and on the seven 002 misses (all now remedied): `FD_COMPOSITION` reduced+asserted at suite:159-161; `POOL_PRESETS` asserted :163; `tokenPoolHeadroom` called+asserted :172; `dayBeforeISO`/`addYearsISO` called+asserted :760-761; `ENTITY` field-asserted :150-152; `CONSTITUTION_DEFAULT`/`TOKEN_POOLS_DEFAULT` :153-156; `GRANT_LIFECYCLES`/`DOC_STATUSES` membership+ordering :362-364; `VALUE_BANDS_DEFAULT` :538-539; `setList` :265-267; `currentRoundStep` :365-367. Each named assertion exercises the export itself. Panel 002's R5.1 FAIL is **remediated**.

## R5.3 — mirror convention on the three highest engine-touching merged PRs — **PASS**

`env -u GH_TOKEN gh pr view N --json state,mergedAt,files`:

- **#80** (COM-151, MERGED 2026-06-10T18:27:46Z): files = `engine/engine.v2.test.mjs`, `scaffold/src/engine.ts`, `Advisors.vue`, `Proposition.vue` — suite touched ✓
- **#79** (COM-153, MERGED 18:20:45Z): files = `engine/engine.v2.test.mjs`, `scaffold/src/engine.ts` — suite touched ✓
- **#77** (COM-150 engine, MERGED 17:55:38Z): files = `engine/engine.v2.test.mjs`, `scaffold/src/engine.ts` — suite touched ✓
- Sample validity checked adversarially: **#78** (COM-150 UI, MERGED) = `PackageEditor.vue` + `Configure.vue` only — does NOT touch engine.ts, correctly excluded; so #80/#79/#77 are exactly the three highest-numbered merged engine-touching PRs. 3/3, no expansion triggered.

## R3.16-partial (live slice) — exercise mechanics on /advisors + fine print on /proposition — **PASS**

Playwright probe (`/tmp/claude/pw/grade003-r316.mjs`) against the fresh-built preview, chromium headless — 22/22 assertions PASS:

1. **Dist hash:** served HTML references `assets/index-BVWx4TsT.js` = the just-built bundle.
2. **/advisors → Instruments tab** (role=tab, count 1, clicked): "Exercise mechanics" section label renders; **all four `EXERCISE_MECHANICS` statements render verbatim** (windows · Option Certificate 3.6 · Plan Rule 4.5 · Plan Rule 7.4(a)); the full 3.6 sentence ("If no Exit Event occurs before the 9th anniversary of grant, the Company shall open an Exercise Period of at least 90 days, on at least 30 days' written notice, ending no later than the day before the 10th anniversary.") is present; the carve-out line renders below the block.
3. **This package's backstop:** renders "If no exit by **Jun 2035**, a ≥90-day window must open, closing by **May 2036**." for the selected package (grant start 2026-06-01). Cross-checked structurally against the engine's own helpers: `fDate(addYearsISO('2026-06-01', 9))` = Jun 2035 ✓ and `fDate(dayBeforeISO(addYearsISO(start, 10)))` = May 2036 ✓ — and consistent with the suite's T12 vector (2035-06-01 / 2036-05-31).
4. **/proposition fine print** carries: the liquidity-windows + 3.6 backstop sentence (with ≥30 days' notice + day-before-10th-anniversary close + "(Option Certificate 3.6)") · net exercise "(Rule 4.5)" · sell-to-cover "(Rule 7.4(a))" · the `FUNDING_ROUND_CARVEOUT` verbatim (Rules 11.2/11.3) · "Not a binding offer" retained.
5. **Hygiene:** localStorage byte-identical after the flow (read-only, nothing to restore); zero console-error entries across both routes.

Screenshots: `/tmp/claude/pw/grade003-advisors-instruments.png` (shows the block under the "Internal & confidential · net of strike, pre-tax · discussion draft, not a binding offer" posture line), `/tmp/claude/pw/grade003-proposition.png`.

Scope note: the money-visible toggles of full R3.16 (pre-TGE 1:1 fallback, cash-floor trade) are COM-152/154 — not yet built, not graded, R3.16 itself remains OPEN.

## R4.7-slice — editorial coherence of new copy since 002 — **PASS** (two non-blocking citations)

All new user-facing copy read in full: the four `EXERCISE_MECHANICS` texts + `FUNDING_ROUND_CARVEOUT` (engine.ts:802-810), the Advisors backstop line + "Exercise mechanics" label, the PackageEditor band quick-selects (`Base · $50,000/yr` etc. from `VALUE_BANDS_DEFAULT`), the Configure "Value bands · annual $ (the negotiation unit)" section + its open-decision-#2 note, and the Proposition fine-print expansion.

- **Sentence case:** holds everywhere ("Exercise mechanics", "This package's backstop", "Value bands · annual $ (the negotiation unit)"). ✓
- **Subject-first:** holds — "Exercise is available…", "Net exercise is available…", "Sell-to-cover is holder-elected…", "Primary financings … trigger no compensatory adjustment…", "Anchor numbers are pending Robin + Carl Sjöström review…". The 3.6 backstop sentence is conditional-first, consistent with the ported legal-corpus register. ✓
- **Confidentiality posture:** the Advisors surface sits under the standing "Internal & confidential · net of strike, pre-tax · discussion draft, not a binding offer" eyebrow (screenshot evidence); the Proposition fine print retains "Not a binding offer or legal/financial advice." and the CONFIDENTIAL_EYEBROW import is untouched. No confidential-posture regression. ✓

**Citations (non-blocking — neither violates the three graded axes):**
1. **Voice drift, engine.ts:803:** the windows text ends "— any time you like, as long as it is during a liquidity event." This is the only second-person, wry sentence in the corpus (provenance: a session quote, spec v2 line 516, rendered unattributed in app voice). It renders on the internal Instruments panel only — the Proposition's version omits it — so advisor-facing surfaces are unaffected. Recommend either quote-attributing it or dropping the gloss.
2. **Term inconsistency, Advisors.vue backstop line:** "If no **exit** by Jun 2035…" uses lowercase "exit" where the corpus defined term is "Exit Event" (the verbatim 3.6 sentence using the defined term renders directly above it in the same block). Cosmetic; recommend aligning to the defined term.

---

## Scoped verdict summary

| Criterion | Verdict |
|---|---|
| R2.1 | PASS |
| R2.2 (FULL) | PASS (167 passed, 0 failed, 0 pending(v2)) |
| R2.3 | PASS |
| R2.4 (so-far) | PASS |
| R2.5 | PASS |
| R5.1 | PASS (002's 7-export FAIL remediated; 34/34 by-name, direct exercises verified) |
| R5.3 | PASS (3/3 mirror, sample validity verified via #78) |
| R3.16-partial (live) | PASS (22/22 probe assertions; full R3.16 stays OPEN pending COM-152/154) |
| R4.7-slice | PASS (2 non-blocking editorial citations: the second-person windows gloss; "exit" vs "Exit Event") |

9/9 PASS. The engine wave's test debt is fully retired at this checkpoint: zero pendings, all T5 live, anchors hold, SCHEMA untouched, mirror convention 3/3, and the panel-002 R5.1 failure is fixed with direct by-name vectors. Remaining wave work (COM-152/146/154/171) is scope, not regression.
