# Rubric grade 004 — Wave 1 final (engine wave complete)

- **Panel:** Wave-1 boundary grader after the full M10 engine wave (PRs #71–#84) — single independent fresh-context grader, adversarial, read-only on the repo
- **Date:** 2026-06-10
- **Branch:** `claude/frosty-pasteur-8cf1db`
- **Frosty tip at grading:** `c5881765b414555c6a81c28642d31f8925cbed06`
- **Scope (pinned by the kickoff):** R2.1 · R2.2 (full) · R2.3 · R2.4 (FULL form) · R2.5 · R5.1 · R5.3 (full population) · R1.1-slice (M10 engine) · R6.1 (so-far) · R3.1 (live). Not a full panel.
- **Working tree at grading:** `git status --porcelain` → empty (clean; the formerly-untracked `OBJECTIVES_AND_FLOWS.md` is gone from the tree).
- **Probes:** all probe scripts run from `/tmp/claude/pw` (`anchors-assert.mjs`, `grade004-r31.mjs`, `grade004-r31-cross.mjs`); no repo file touched outside `docs/rubric-grades/`.

## Dist-freshness precondition — SATISFIED

- `cd scaffold && npm run build` → exit 0, `✓ built in 3.55s`, emits `dist/assets/index-BDyAcZmk.js` (980.12 kB / gzip 289.75 kB) + `dist/assets/index-sScgB_s_.css`.
- `fetch('http://[::1]:4173/')` → HTML references `assets/index-BDyAcZmk.js` + `assets/index-sScgB_s_.css` — exact hash match with the fresh build. All live grading ran against this bundle.

---

## R2.1 — both 22-vector suites green — **PASS**

- `node engine/engine.test.mjs` → `22 passed, 0 failed.`
- `node scaffold/engine.test.mjs` → `22 passed, 0 failed.`
- Immutability (RFC §7 context, checked adversarially): `git diff --stat 3bf3e20..HEAD -- engine/engine.test.mjs scaffold/engine.test.mjs` → empty. Both v1 suites byte-identical to baseline.

## R2.2 (full) — v2 suite 0 failed AND zero PENDING(v2) — **PASS**

- `node engine/engine.v2.test.mjs` → **`215 passed, 0 failed, 0 pending(v2).`**
- `grep -c 'PENDING(v2)'` on the captured run output → `0`. Adversarial case-insensitive sweep: the only 3 `pending`-matching lines are two PASS vector names about `pendingUplift`/gated intros and the `0 pending(v2)` summary line — no pending assertions remain.
- All bindings run LIVE against the real engine (suite line 99: `const ENG = await import(new URL('../scaffold/src/engine.ts', import.meta.url).href)`).

## R2.3 — A.3 anchors reconcile in the LIVE engine — **PASS**

One-off node script (`/tmp/claude/pw/anchors-assert.mjs`, node v24.16.0 importing `scaffold/src/engine.ts` directly) calling `walkScenario(DEFAULT().plan, 'base')`:

| Anchor | Got | Want | Delta |
|---|---|---|---|
| bridge N | 57,217.2395 | 57,217 ±1 | +0.2395 |
| Series A N | 75,359.2910 | 75,359 ±1 | +0.2910 |
| Series B N | 89,379.6242 | 89,380 ±1 | −0.3758 |
| Series C N | 118,707.3134 | 118,707 ±1 | +0.3134 |
| bridge price | $1,572.9525 | $1,572.95 ±$0.01 | +$0.0025 |

Exit 0, all five PASS.

## R2.4 (FULL form) — SCHEMA changed exactly once — **PASS**

- `git log -G'SCHEMA = ' --oneline 3bf3e20.. -- scaffold/src/engine.ts` → exactly one commit: `1ea8432 feat(engine)!: COM-171 — the single SCHEMA-6 bump (materialise grants[], delete cocAccelPct, loss-free v5→v6)`.
- `gh pr view 84 --json commits,state` → MERGED; `1ea8432970…` is the **sole** commit of PR #84 (COM-171). No SCHEMA-touching commit outside that PR.
- `grep -c 'SCHEMA = 6' scaffold/src/engine.ts` → `1`; the only `SCHEMA = ` assignment in the file is line 159 `export const SCHEMA = 6;`.
- COM-171 round-trip vectors pass in the run (T16 · SCHEMA v6 migration), cited by name:
  - **`LOADS AND COMPUTES IDENTICALLY: every money field equal pre/post migration (incl. the CEILINGS)`** — the v5 localStorage-map member path (suite lines 1020–1042: builds a v5 payload with `version = 5`, `plan.cocAccelPct`, no `grants`, asserts per-advisor money equality post-`reconcile`).
  - **`the #s= hash form (raw State) migrates through the same path`** — suite lines 1044–1052: the decodeHash product (raw State) through `reconcile`, asserting `version === 6`, `cocAccelPct` deleted, and board cost parity to 1e-6.
  - Supporting: `SCHEMA is 6 — the one and only bump of the wave`, `cocAccelPct is gone from DEFAULT() and deleted from migrated payloads`, `idempotent: reconcile(reconcile(v5)) — derived rows refresh, never duplicate` — all PASS.
- Minor note (not a failure): the `#s=` vector emulates the `decodeHash` JSON product directly rather than decoding a literal base64 hash string; `store.ts` routes both forms through the same `reconcile` call the vector exercises.

## R2.5 — zero unsanctioned money math in views/components — **PASS**

Closest read on every surface changed in Wave 1 (`git diff --name-only 3bf3e20..HEAD` on src, engine excluded: `PackageEditor.vue`, `VestingTimeline.vue`, `Advisors.vue`, `Configure.vue`, `Proposition.vue`, `store.ts`):

- **Configure.vue (value bands + cash floor panel + pool/constitution):** all Wave-1 additions read engine exports (`poolGuardrail`, `poolSharesExact`, `tokenPoolHeadroom`, `POOL_PRESETS`, `FD_COMPOSITION`, `ENTITY`) + formatting helpers. No view arithmetic added.
- **PackageEditor.vue (floor input + band chips):** band chips print `b.label · fUSD(b.annualUSD)` verbatim; the floor panel displays `c.cashFloorTraded` / `c.cashFloorFrac` straight from `computeAdvisor`. No view math.
- **Advisors.vue (exercise mechanics + grant rows + token-value row):** all pricing flows through `effectiveGrants`/`computeGrant`/`exerciseCheck` (lines 90–113); the token-value row reads `c.base.token` directly (line 426) — the in-template comment at lines 418–419 documents that the old `tkPct×fdv` mirror was deliberately abandoned because it diverges under the COM-152 pre-TGE fallback. Exercise cost is `fUSD(c.exerciseCost)` (line 410).
- **Proposition.vue / VestingTimeline.vue Wave-1 deltas:** copy/legal text only (the Δ4 v9 no-CoC-acceleration correction + Rules 4.5/7.4(a)/3.6 wording + `FUNDING_ROUND_CARVEOUT` import). No money-math additions.
- Full-tree grep (`scaffold/src/views` + `scaffold/src/components`) for `*`/`/` on money identifiers: every surviving hit is a pre-baseline **sanctioned mirror** of a cited engine formula — `Board.vue:107` (`s.netEqAt(c.eqPctCeil, s.exitVal) + c.tkPctCeil * s.fdv`) ↔ `engine.ts:778`; `VestingTimeline.vue:18` (`c.tkPct * sbv.fdv`) ↔ `engine.ts:776–777`; `PackageEditor.vue:218–219` / `Configure.vue:908` (`baseGrant.×Pct * t.mult`, tier preview) ↔ `engine.ts:690` (`tierMult = t.mult`); `Proposition.vue:49` (`cc.baseEq * b.grantN`) ↔ `engine.ts:762` (`const equityShares = eqPct * sb.grantN`); `UpsideCurve.vue:27` (`eqPct * (retention*V − strikeBasis)`) ↔ `engine.ts:754` (`netEqAt`). Zero violating lines to cite.

## R5.1 — every new runtime export has a by-name vector — **PASS**

- New value exports since baseline (set-diff of `export function|const|class` names, `3bf3e20` vs HEAD): **40** — `addYearsISO, BAD_LEAVER_LIMBS, capitalRollup, CASH_FLOOR_DEFAULT, classifyLeaver, computeGrant, CONSTITUTION_DEFAULT, currentRoundStep, dayBeforeISO, distributableFrac, DOC_STATUSES, effectiveGrants, ENTITY, EXERCISE_MECHANICS, exerciseCheck, FD_COMPOSITION, fullMonthsBetween, FUNDING_ROUND_CARVEOUT, GRANT_LIFECYCLES, hasExplicitGrants, INTRO_STATUSES, makeScenarioSet, METHOD_NOTES, modelDeparture, PLAN_DISQUALIFICATION_WARNINGS, planWithSet, POOL_PRESETS, poolGuardrail, poolSharesExact, preTgeLiquidity, setList, TOKEN_POOLS_DEFAULT, tokenPoolHeadroom, tokenValueFor, VALUE_BANDS_DEFAULT, valueToQuantity, vestedAtDate, vestedAtMonths, vestedFracRTA, walkComposed` (14 type-only exports exempt).
- All 40 names appear in `engine/engine.v2.test.mjs`; the suite uses a namespace import (`ENG.<name>`), so every occurrence is a live reference, not an import-list mention.
- Adversarial close-read of the five single-reference names — each is the subject of a direct named assertion: `dayBeforeISO` (`date helpers are TZ-free and junk-safe` — `ENG.dayBeforeISO('2036-03-01') === '2036-02-29'`), `FD_COMPOSITION` (`FD composition (A.2) sums to 48,316.78…` — reduce over `ENG.FD_COMPOSITION`), `POOL_PRESETS` (`pool presets print the workbook cells: 10% → 5,368 · 15% → 8,523`), `tokenPoolHeadroom` (`Advisors token headroom = 1.82552%` — `ENG.tokenPoolHeadroom(pools.advisors)`), `INTRO_STATUSES` (`INTRO_STATUSES: targeted → gated → earned`). Spot-checks on `METHOD_NOTES`, `walkComposed`, `vestedAtMonths` likewise exercise the export itself.

## R5.3 (full-population) — the mirror convention held on all 11 wave PRs — **PASS**

`env -u GH_TOKEN gh pr view N --json state,files` for #71, #73, #74, #76, #77, #79, #80, #81, #82, #83, #84: **all MERGED, and every one touched BOTH `scaffold/src/engine.ts` AND `engine/engine.v2.test.mjs`** (engine=1/suite=1 on each).

Completeness cross-check: `git log --oneline 3bf3e20..HEAD -- scaffold/src/engine.ts` → exactly 11 commits (`f12b5bd`→COM-142/#71, `2095994`→COM-143/#73, `e4c8223`→COM-144/#74, `2a4944c`→COM-145/#76, `a9b939e`→COM-150/#77, `1976a5b`→COM-153/#79, `3bc0ca8`→COM-151/#80, `eaabe46`→COM-152/#81, `ad89b97`→COM-146/#82, `edeca2d`→COM-154/#83, `1ea8432`→COM-171/#84). No engine-touching commit outside the 11 audited PRs. Zero misses.

## R1.1-slice (M10 engine) — Linear milestone state — **PASS**

`list_issues(project="Advisor Comp Studio — Web App (Frappe/Vercel)", limit=250)` → 171 issues, `hasNextPage: false` (population exhausted). Milestone `M10 · Engine v2 — value grants, scenario sets, legal mechanics (spec-first)` holds 16 issues:

- **Done (12):** COM-140, COM-142, COM-143, COM-144, COM-145, COM-146, COM-150, COM-151, COM-152, COM-153, COM-154, COM-171.
- **Duplicate (1):** COM-87.
- **Backlog (3):** COM-147 (`Configure: scenario-set editor…`), COM-148 (`Global scenario-set switcher…`), COM-149 (`VestingTimeline: render both instrument curves…`) — exactly the exempt M10 UI trio (Wave 2), verified by title to be the UI issues.

Zero non-exempt issues with statusType backlog/unstarted/started in the M10 milestone.

## R6.1 (so-far) — bundle budget — **PASS** (thin gzip margin — watch in Wave 2)

- `scaffold/dist/assets/*.js`: one chunk, `index-BDyAcZmk.js` — **minified 980,802 B** (limit 1,000,000 B; 19,198 B headroom, 98.1% of budget) · **gzip 288,896 B** (limit 290,000 B; **1,104 B headroom, 99.6% of budget**).
- Delta vs baseline (824,954 B / 241,008 B): **+155,848 B minified (+18.9%) · +47,888 B gzip (+19.9%)** for the whole M10 engine wave.
- Verdict is PASS as specified, but the gzip ceiling is one mid-size PR away — the R6.1 escape hatch (route-level code-splitting) should be on the Wave-2 critical path, not deferred to the end of the run.

## R3.1 (live) — value-denominated grant drives the derived rows — **PASS**

Probe `/tmp/claude/pw/grade004-r31.mjs` (playwright, chromium headless, against the hash-matched `:4173` bundle):

1. **Snapshot:** all localStorage captured pre-flow (fresh context → empty; written to `grade004-ls-snapshot.json`).
2. **Add advisor:** Board header `Add` → roster 4 → 5 (note: the populated-board affordance is `Add` on `/board`; `Add advisor` only exists on empty states). SPA-nav to Advisors (a hard `goto` would reset `selId` to advisors[0] — caught adversarially on a first run that edited Iraj Ispahani instead).
3. **Edit package:** dialog title `Edit package · New advisor` (the right advisor).
4. **Switch to `By $ value`**, set **Annual value = $250,000** via the labeled NumIn (Enter commits). Live readback: `Resolves to 0.72% eq · 0.058% tok at the base-case path`.
5. **Instruments tab derived rows (DOM):** 2 rows — `Jun 2026 · Bridge · strike $1572.95 · FMV $1572.95 · count 413 · $1.09M · granted` and `Jun 2026 · Bridge · strike — · FMV $0.060 · count 5.8M · $350K · granted`.
6. **Engine cross-check of the SAME persisted state** (`grade004-r31-cross.mjs`, importing `scaffold/src/engine.ts` over the captured localStorage JSON): `effectiveGrants` → **2 rows** (`option q=413.2356…`, `rta q=5,833,333.33…`); `computeAdvisor.equityShares = 413.2356…` ≡ the option quantity; `strikePps = 1572.9525…`. DOM row count 2 = engine row count 2; rendered `413` = round(413.236); rendered `5.8M` = 5,833,333 within display rounding; rendered strike `$1572.95` = engine `$1,572.9525`. (Per the criterion's note, the `$value/(FMV−strike)` form is degenerate at bridge where FMV=strike — the row-count + quantity cross-check against the engine's own `computeAdvisor` is the binding assertion, and it agrees.)
7. **Restore:** snapshot written back, reload — byte-exact localStorage equality (`restoredExact: true`) AND semantic state equality (`stateEqual: true`); roster back to pre-flow count. **Zero console errors** across the whole flow.

---

## Verdict summary

**10 of 10 graded criteria PASS** at frosty tip `c588176`: both v1 suites 22/22 and byte-identical to baseline; the v2 suite 215/0/0-pending with all bindings live; all five A.3 anchors inside tolerance in the live engine; SCHEMA bumped exactly once in PR #84's sole commit with both named v5→v6 round-trip vectors green; no unsanctioned money math on any Wave-1 surface (every surviving view-side expression mirrors a cited engine line); all 40 new runtime exports vectored by name; the engine↔suite mirror held on all 11 wave PRs with no engine commit outside them; the M10 milestone is exactly 12 Done + COM-87 Duplicate + the exempt COM-147/148/149 UI trio; the bundle is inside budget at 980,802 B / 288,896 B gzip — **but with only 1,104 B (0.4%) of gzip headroom left, the R6.1 code-splitting escape hatch is effectively due at the start of Wave 2**; and the R3.1 dollar-value flow drives the Instruments derived rows in exact agreement with the engine's own computeAdvisor on the same state, with localStorage restored byte-exact and zero console errors.
