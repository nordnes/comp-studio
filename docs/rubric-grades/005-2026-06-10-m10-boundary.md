# Rubric grade 005 — M10-milestone boundary panel

- **Panel:** independent adversarial grader (fresh context), single-agent boundary panel
- **Date:** 2026-06-10
- **Tip:** `fc6d43c5daa1a0c7e3450ec43852082453911a52` (frosty — "docs(memory): ★ Wave 2 / M10 milestone complete")
- **Dist freshness:** `cd scaffold && npm run build` re-run by the grader → entry `index-DBYaw9mn.js`;
  `http://[::1]:4173/` confirmed serving the SAME hash before any preview grading.
- **Probes:** `/tmp/claude/pw/grade005-*.mjs` (written by this panel, not reused from the builder).
  Read-only on the repo; localStorage snapshot/restore proven byte-identical.

## Verdicts

### R1.1 (M10 slice) — PASS
`list_issues(project="Advisor Comp Studio — Web App (Frappe/Vercel)", limit=250)` returned **171
issues with `hasNextPage:false`** — exhaustive in one page. Client-side group on
`projectMilestone.name` starting `M10 ·` → **16 issues**: 15 `Done` (COM-140, 142, 143, 144, 145,
146, 147, 148, 149, 150, 151, 152, 153, 154, 171) + **COM-87 `Duplicate` with evidence**: Robin's
2026-06-10 closing comment cites the tombstone condition, PR #73's `walkComposed` +
`ScenarioSet`/`planWithSet`, and the residual-requirement check. Grader verified the citations:
`gh pr view 73` = MERGED; `scaffold/src/engine.ts:588` exports `walkComposed`. Zero M10 issues
with statusType backlog/unstarted/started. (M11/M12 were out of this panel's scope per the
boundary instruction.)

### R2.1 — PASS
- `node engine/engine.test.mjs` → `22 passed, 0 failed.`
- `node scaffold/engine.test.mjs` → `22 passed, 0 failed.`

### R2.2 — PASS
`node engine/engine.v2.test.mjs` → **`217 passed, 0 failed, 0 pending(v2).`** — and
`grep -c 'PENDING(v2)'` over the run output = **0**.

### R2.3 — PASS
One-off node import of the LIVE engine (`/tmp/claude/pw/grade005-anchors.mjs`, node v24 strip-types,
`walkScenario(DEFAULT().plan,'base')`):
| anchor | bound | live value | |
|---|---|---|---|
| bridge N | 57,217 ±1 | 57,217.239 | PASS |
| Series A N | 75,359 ±1 | 75,359.291 | PASS |
| Series B N | 89,380 ±1 | 89,379.624 | PASS |
| Series C N | 118,707 ±1 | 118,707.313 | PASS |
| bridge price | $1,572.95 ±$0.01 | $1,572.9525 | PASS |

### R2.4 (full) — PASS
- `git log -G'SCHEMA = ' --oneline 3bf3e20.. -- scaffold/src/engine.ts` → **exactly one commit**:
  `1ea8432` "feat(engine)!: COM-171 — the single SCHEMA-6 bump". `gh pr view 84` confirms PR #84
  (MERGED, branch `robinandre/com-171-…`) contains **only** that commit (`1ea8432970…`).
- `grep -c 'SCHEMA = 6' scaffold/src/engine.ts` = **1** (line 159 — the only `SCHEMA = ` line).
- v5→v6 round-trip vectors pass by name in the T16 section of the v2 run output:
  - `LOADS AND COMPUTES IDENTICALLY: every money field equal pre/post migration (incl. the CEILINGS)` (the v5 localStorage map)
  - `the #s= hash form (raw State) migrates through the same path` (the v5 `#s=` hash)
  - plus `cocAccelPct is gone from DEFAULT() and deleted from migrated payloads`,
    `idempotent: reconcile(reconcile(v5)) — derived rows refresh, never duplicate`,
    `migration materialises derived-flagged grants for parametric advisors (option + rta rows)`,
    `explicit grants are untouched by the migration; [] stays explicit-zero`.

### R3.4 (live, full) — PASS (16/16, `grade005-r34.mjs`)
Complete flow driven on :4173 against the fresh dist:
1. **Save** → set "Grader Alpha" persisted (LS-verified).
2. **Duplicate** → "Grader Alpha (copy)".
3. **Star** → `aria-pressed` false→true on the duplicate; exactly one starred in LS.
4. **UI money edit** (NumIn click-to-edit): base `seriesC.post` 500M→400M, then saved as a second
   distinct set "Grader Beta".
5. **Global switch via the shell Set lens** (`aria-label="Scenario set"`): the LABELED money figure
   on /advisors changed **Base $6.76M → Base $7.67M**; "Switched to "Grader Alpha"" toast fired.
6. **Diff two sets on Compare** (Set A = Grader Alpha, Set B = Grader Beta): non-zero deltas
   rendered — Board cost **$23.0M → $20.3M ($-2.71M)**, Iraj $-904K, others $-603K.
7. **A/B one advisor**: Package A = Iraj Ispahani → **Fork B** → Package B = "Iraj Ispahani (B)";
   Base/Ceiling/Best-case comparison rows rendered; the fork persisted in the roster.
8. **Zero trace**: restore + reload → localStorage **byte-identical** to the pre-flow snapshot;
   no sets, no forked advisor remain. **Zero console errors across the whole flow.**

### R3.3 (live) — PASS (12/12, `grade005-r33.mjs` + screenshot)
/advisors → Vesting tab. SVG geometry (constants from the component: baseline y=188, x(12)=164.5,
x(24)=293), curves identified by paint, not paint order:
- **Token DISTRIBUTABLE area flat-zero before month 24**: 94 vertices left of x(24), max deviation
  **0.000 px** from the baseline; the area rises only after the gate.
- **Dashed accrual line rises from the cliff**: flat at baseline before x(12), jumps to y=154.0 at
  the cliff, monotonic rise to y=62.6 at m48 (`stroke-dasharray="5 3"` — the non-color channel).
- **Distinct treatments**: the equity tread is flat between anniversaries (annual staircase) while
  the accrual is a 49-vertex monthly ramp; legends name all three ("Base equity (annual
  staircase)" · "Tokens distributable" · "Token vesting (accruing)"); the caption states the
  24-month qualifying gate. Zero console errors.

### R4.1/R4.2/R4.4 slice (new Wave-2 surfaces) — PASS, one out-of-slice observation
Screenshots: `grade005-r4-configure-headlines.png`, `grade005-r4-configure-sets.png`,
`grade005-r4-compare-diff-ab.png` (visually reviewed by the grader).
- **R4.1 layout (DS §2)**: /configure = `max-w-reading` (computed 960px — §2.1 reading column);
  /compare = `max-w-7xl` (computed 1280px — the §2.1 sanctioned dense-table opt-out). All three new
  cap-group sections ("Headline observations · this path", "Walk priors · re-base rounds on another
  path", "Scenario sets · saved bundles") and both new Compare sections ("Scenario sets ·
  side-by-side", "A/B · two packages, one person") carry `.section-label` headings in sentence case
  (§2.5). Diff rows are label+`divide-y` read-outs on the canvas — borders earn their place (§2.2);
  set cards are interactive editors, so their frames are sanctioned.
- **R4.2 amber (DS §1)**: the two new Compare sections contain **zero** amber/orange elements
  (scoped DOM sweep). /configure ambers are all conforming: starred-set/base-scenario
  `border-outline-amber-2` (active state), `ink-amber-strong` on the TGE working-assumption warning
  (status), NumIn hover accent, the Internal eyebrow (brand). **Observation (outside this slice,
  pre-existing M4 surface)**: the Compare roster matrix's tier `Badge theme="orange"`
  (`Compare.vue:316-319`, "Anchor"/"Strategic") uses the accent for tier *classification* — worth a
  look from the next full-R4.2 panel; not a Wave-2 surface, so not graded here.
- **R4.4 slice**: real Tab-walk reaches the shell Set lens (frappe-ui Select, `role=combobox`) and
  the star button (visible focus ring confirmed via computed style; `aria-pressed` present and
  flipping). Set A/Set B/Package A/Package B/Evaluate-under selects all labeled + focusable; Fork B
  a real button. Icon-only buttons: **0 missing accessible names** on /configure and /compare
  (e.g. "Archive set", "Delete scenario" all aria-labeled).

### R6.1 (corrected form) — FAIL (clause 1, marginal) · clause 2 PASS
- **Initial-load payload (cold /overview, network-captured)** — 7 chunks fetched:
  `index-DBYaw9mn.js` 230,696 B + `Overview` 4,047 + `PoolAllocation` 1,600 + `Avatar` 1,235 +
  `PageHeader` 741 + `EmptyState` 608 + `Panel` 424 = **239,351 B gzip ≤ 290 kB** → **PASS**
  (17.5% headroom; local `gzip` default level, per the criterion's measure).
- **Total minified `dist/assets/*.js`** = **1,004,510 B**. The rubric's operative unit convention
  is SI — its own inline numbers (288,896 B graded as "0.4% headroom" under 290 kB by panel 004)
  pin kB = 10³ B, hence 1.0 MB = 10⁶ B. **1,004,510 B exceeds the 1.0 MB ceiling by 4,510 B
  (0.45%)** → **FAIL** on hard numbers. (Under a binary MiB reading it would pass at 0.958 MiB —
  recorded for transparency, but an adversarial grader cannot adopt the looser unit against the
  panel-004 precedent. Largest offsets: `index-DBYaw9mn.js` 762,293 B, `FrappeChart` 78,582 B,
  `Advisors` 46,761 B — ~5 kB of any further splitting/tree-shake clears it.)

### R6.2 slice — PASS
Cold context per route, `performance.getEntriesByType('paint')` first-contentful-paint:
/overview 136 ms · /board 144 · /compare 144 · /governance 148 · /advisors 136 ·
/proposition 236 · /configure 140 — **all < 2,000 ms**.

### R8.5 (prod) — FAIL (persistent mismatch after the re-poll)
- Local fresh build at the frosty tip: **`index-DBYaw9mn.js`**.
- `https://comp-studio-one.vercel.app` fetch #1: serves **`index-BBEXHKjd.js`** (the pre-PR-#88
  bundle — the same lag memory.md recorded at Wave-2 close: "prod serves the pre-#88 bundle
  (BBEXHKjd vs tip DBYaw9mn) … re-verify before Wave 3's first merge").
- **Re-poll after 120 s: still `index-BBEXHKjd.js`.** The auto-deploy has NOT caught up →
  **FAIL** per the criterion's persistent-mismatch rule. Both hashes: prod `index-BBEXHKjd.js`
  vs frosty-tip build `index-DBYaw9mn.js`. Remediation is a deploy of the tip (the
  memory-flagged pending prod check), not a code change — but R8.5 binds on what prod serves.

## Panel summary
| Criterion | Verdict |
|---|---|
| R1.1 (M10) | PASS |
| R2.1 | PASS |
| R2.2 | PASS |
| R2.3 | PASS |
| R2.4 | PASS |
| R3.4 | PASS |
| R3.3 | PASS |
| R4.1/4.2/4.4 slice | PASS (1 out-of-slice observation) |
| R6.1 | **FAIL** (total minified 1,004,510 B > 10⁶ B; initial-load clause passes) |
| R6.2 slice | PASS |
| R8.5 | **FAIL** (prod `index-BBEXHKjd.js` ≠ tip `index-DBYaw9mn.js`, persisted through the 120 s re-poll) |

**Boundary verdict: 9 of 11 graded items PASS; 2 FAIL — R6.1 (total minified 1,004,510 B, 0.45%
over the SI 1.0 MB ceiling; the user-paid initial payload is fine at 239,351 B) and R8.5 (prod
still serving the pre-#88 bundle after the re-poll). Both have narrow, known remedies: ~5 kB of
JS off the total, and a production deploy of the frosty tip.**
