# Rubric grade 006 — M11 boundary (Wave 3 close)

- **Panel:** independent adversarial grader (fresh context), single-agent boundary panel 006
- **Date:** 2026-06-10
- **Tip SHA:** `75ffbc7fab149e0f294105c0f169aad343a99798` (frosty, `75ffbc7`)
- **Scope:** the 12-item M11-boundary slice ordered by the kickoff (R2.1–R2.4 · R1.1 git-substituted ·
  R5.1/R5.3 Wave-3 · R2.5 Wave-3 surfaces · R3.5/R3.6 live · R4.1/R4.2/R4.4 Wave-3 surfaces ·
  R6.1/R6.2 · R8.5). Linear MCP DOWN → R1.1 graded from git/PR evidence per the kickoff
  substitution; no Linear-dependent check was attempted.
- **Dist freshness:** `npm run build` re-run before grading; preview `http://[::1]:4173` serves
  `index-KMTdnIAr.js` == the fresh `dist/assets/index-KMTdnIAr.js` (hash match confirmed).
- **Probes:** `/tmp/claude/pw/grade006-*.mjs` (+ screenshots `grade006-*.png`). Repo untouched
  outside this file; `vp check --fix` never run.

---

## R2.1 — PASS
- `git diff 3bf3e20 -- engine/engine.test.mjs scaffold/engine.test.mjs` → **EMPTY** (0 lines).
- `node engine/engine.test.mjs` → `22 passed, 0 failed.`
- `node scaffold/engine.test.mjs` → `22 passed, 0 failed.`

## R2.2 — PASS
- `node engine/engine.v2.test.mjs` → **`253 passed, 0 failed, 0 pending(v2).`**
- `grep -c 'PENDING(v2)'` over the run output = **0** lines. All bindings live.

## R2.3 — PASS
One-off node v24.16.0 import of `scaffold/src/engine.ts` (erasable TS, direct import),
`walkScenario(DEFAULT().plan, 'base')`:

| Anchor | Got | Bound | Verdict |
|---|---|---|---|
| bridge N | 57,217.2395 | 57,217 ±1 | PASS |
| Series A N | 75,359.2910 | 75,359 ±1 | PASS |
| Series B N | 89,379.6242 | 89,380 ±1 | PASS |
| Series C N | 118,707.3134 | 118,707 ±1 | PASS |
| bridge price | $1,572.9525 | $1,572.95 ±$0.01 | PASS |

## R2.4 — PASS
- `git log -G'SCHEMA = ' --oneline 3bf3e20.. -- scaffold/src/engine.ts` → exactly **one** commit:
  `1ea8432` ("feat(engine)!: COM-171 — the single SCHEMA-6 bump").
- `1ea8432` is the **sole commit of PR #84** (MERGED, body "Fixes COM-171") — verified via
  `gh pr view 84 --json commits` → `["1ea8432"]`.
- `grep -c 'SCHEMA = 6' scaffold/src/engine.ts` = **1** (line 246).
- The COM-171 round-trip vectors run inside the green 253-vector suite (R2.2).

## R1.1 (M11 slice, git-substituted) — PASS *(substitution: Linear MCP down)*
Graded from `env -u GH_TOKEN gh pr view N --json state,body` instead of Linear `list_issues` —
the Linear-side `Done` status of COM-155..165 was NOT verifiable this panel and remains open for
the next panel with a live MCP.

| PR | State | Fixes |
|---|---|---|
| #90 | MERGED | COM-155 |
| #91 | MERGED | COM-159 |
| #92 | MERGED | COM-160 |
| #93 | MERGED | COM-157 |
| #94 | MERGED | COM-158 |
| #95 | MERGED | COM-162 |
| #96 | MERGED | COM-163 |
| #97 | MERGED | COM-161 |
| #98 | MERGED | COM-164 |
| #99 | MERGED | COM-156 |
| #100 | MERGED | COM-165 |

Coverage = exactly {155,156,157,158,159,160,161,162,163,164,165}; each PR exactly one `Fixes`
line; no gaps, no duplicates, no extra issues.

## R5.1 (Wave-3 exports) — PASS
- Wave-2 tip = `3bcfb0d` (= parent of PR #90's first commit `bfb98ba`; merge of #89).
- `git diff 3bcfb0d..HEAD -- scaffold/src/engine.ts` adds **17 runtime value exports** (the
  residual grep for any further `+export` line not in this list, net of type/interface erasures,
  is EMPTY): `DOC_STATUSES · CHECK_STATUSES · CONTRACTING_STRUCTURES · REVIEW_OUTCOMES ·
  ADVISOR_STAGES · advisorStage · ISPAHANI_STEPS · seedBoard · trajectoryBand · trajectoryEvents ·
  ADVISORY_MEDIAN_USD · BENCH_DAY_RATE_USD · generosityCheck · makeProposition ·
  crystalliseIntroductions · addMonthsUTC · nextReviewDue`.
- Every one is exercised **by name on the live import** (`ENG.<name>`) in
  `engine/engine.v2.test.mjs` — counts: seedBoard 1 · addMonthsUTC 2 · advisorStage 4 ·
  DOC_STATUSES 3 · CHECK_STATUSES 1 · CONTRACTING_STRUCTURES 1 · REVIEW_OUTCOMES 1 ·
  ADVISOR_STAGES 1 · ISPAHANI_STEPS 5 · trajectoryBand 3 · trajectoryEvents 4 · generosityCheck 7
  · makeProposition 2 · crystalliseIntroductions 2 · nextReviewDue 4. The two constants are
  asserted directly (suite line 1515: `ENG.ADVISORY_MEDIAN_USD === 50000 &&
  ENG.BENCH_DAY_RATE_USD === 10000`) — the export itself, not a caller.

## R5.3 (Wave-3 population) — PASS
`gh pr view N --json files` across #90–#100:
- **Engine-touching** (list `scaffold/src/engine.ts`): #90, #91, #92, #93, #95, #98, #99, #100 —
  **all eight also touch `engine/engine.v2.test.mjs`** in the same PR. Zero misses.
- **UI-only** (exempt): #94 (ReviewPanel/store/Advisors/components.d.ts), #96
  (DepartureDialog/Advisors/components.d.ts), #97 (CapitalRollupPanel/PackageEditor/store/Board/
  components.d.ts) — none lists `scaffold/src/engine.ts`; the exemption is genuine.

## R2.5 (Wave-3 surfaces) — **FAIL** (one cited line)
Close read of TrajectoryView.vue · ReviewPanel.vue · DepartureDialog.vue · CapitalRollupPanel.vue
· GrantDecisionWizard.vue · the Board generosity panel (Board.vue Wave-3 diff) · the Proposition
version register (Proposition.vue Wave-3 diff).

- **Violation — `scaffold/src/components/CapitalRollupPanel.vue:22-25`:**
  ```js
  const coverage = computed(() => {
    const t = roll.value.totals;
    return raiseTarget.value > 0 ? (t.earned + t.gated) / raiseTarget.value : 0;
  });
  ```
  rendered as "earned + gated cover {{ fPct(coverage, 0) }}". This computes a **percent** in a
  view from `plan.bridge.raise` (**raw state**, line 21) — `capitalRollup` (engine.ts:1333)
  returns `rows/totals/schedule` and exports **no coverage figure or formula to mirror**. Under
  the protocol definition ("anything else computing value/price/percent from raw state in a view
  = FAIL R2.5") this is unsanctioned. Severity is low (a display ratio over engine totals; no
  compounding) and the remedy is mechanical (export coverage from `capitalRollup`, which already
  receives the plan) — but the definition is strict and the line is cited. **FAIL.**
- Borderline, judged sanctioned: `Proposition.vue:185-189` `c.baseCaseTotal −
  v.figures.baseCaseTotal` — a same-metric delta between the live engine figure and the
  engine-frozen snapshot (`makeProposition` writes `v.figures`); display semantics, no new money
  semantics.
- Clean: TrajectoryView (SVG x/y scaling + Math.max layout only; band/events all engine),
  ReviewPanel (`Number(v)` input parse only; pricing in store/engine), DepartureDialog (zero
  arithmetic; classifyLeaver/modelDeparture), GrantDecisionWizard (fUSD/fPct/`.toFixed` display of
  engine figures), Board generosity additions (generosityCheck render only; status ternaries),
  CapitalRollupPanel's other expressions (`×100` display of `schedule.pct/cap` — sanctioned).

## R3.5 (live, :4173) — PASS
Probe `grade006-r35.mjs` on the seeded board (advisors[0] = Iraj Ispahani; full Δ9 roster
Reoch/Keller/Derhalli/Bang/Mehta/Ispahani verified on /board by `grade006-roster.mjs`):
1. Schedule review (cadence-projected date) → review row `open`, completion form renders.
2. Completion attempt **without approver** (outcome top-up $64,000): **REFUSED** — review stays
   open (1), grant count unchanged (2→2).
3. Completion **with** approver "Grader Panel 006" + top-up: grant count 2→3, appended grant
   `valueUSD === 64000`; review on the record (`outcome: 'top-up'`, `approver: 'Grader Panel
   006'`, `completedISO` set); UI shows "signed off by Grader Panel 006".
4. localStorage (`raiku-advisor-comp-v5`) snapshot-restored byte-identical; **zero console
   errors**.

## R3.6 (live, :4173) — PASS
Probes `grade006-r36.mjs` + `grade006-r36b.mjs`, cessation date 2028-06-10 (post-cliff):
- **Good leaver** (no limbs): Options 858 granted / 429 vested / **429 retained** (retained ==
  vested), Board-discretion badge present; package retains **$2.70M today / $3.83M at exit**.
- **Limb-1 bad leaver**: Options retained **0**, lapses **858** (vested AND unvested zeroed —
  Rule 5.8); retained collapses to **$0** (the money assertion holds).
- Record → `a.stage === 'rolled-off'` persisted with the stageHistory note "Departure modeled
  2028-06-10 · bad leaver · retained $0 (today basis)"; UI reflects rolled-off.
- localStorage restored; zero console errors.
- Note (correct behaviour, not a bug): the Options **row** "Retained value (today)" reads $0 for
  the good leaver too — net-of-strike at today's bridge-basis FMV (price == strike → zero
  intrinsic); the contrast is carried by the at-exit totals and the RTA/cash legs.

## R4.1 (Wave-3 surfaces) — PASS
- /board: `max-w-7xl` (sanctioned dense-table opt-out, DESIGN_SYSTEM §2.1); /advisors:
  `max-w-reading` ✓.
- `.section-label` headings on every new panel: "Capital introductions · the board as a
  fundraising channel", "Generosity guardrails · the Sjöström check", "Trajectory · vested value
  over the engagement, net of strike", "Reviews · the growth mechanic, on the record" (+ dialog
  titles for the two modals) ✓ (§2.5).
- Borders earn their place (§2.2): trajectory/review/generosity render label+content on canvas;
  the capital panel's frame is a conclusion surface; no nested cards observed.
- Screenshots: `grade006-board-capital.png`, `grade006-board-generosity.png`,
  `grade006-trajectory.png`, `grade006-departure.png`, `grade006-wizard.png`.

## R4.2 (Wave-3 surfaces) — PASS
Template grep + screenshot sweep: amber appears only as status/pending/current-case semantics —
gated capital amounts (`text-ink-amber-strong`), band-breach + pool-consumption warnings,
Board-discretion badge (`theme="orange"`), the departure warnings box
(`border-outline-amber-2 bg-surface-amber-1`), plus the §2.2(b)-sanctioned amber company-cost
conclusion panel. Status always carries a non-color channel (▲◆▼ glyphs, text badges, prose).
No amber headings; labels use `ink-amber-strong` (AA-safe) per §1.

## R4.4 (Wave-3 surfaces) — **FAIL** (one citation, library chrome)
- **Violation — the frappe-ui Dialog-chrome close "X" button** rendered by BOTH new dialogs
  (DepartureDialog, GrantDecisionWizard) has an **empty accessible name** (aria snapshot `""`;
  no aria-label/sr-only/svg-title) and measures **28×28 px** (< the 32 px floor) —
  DESIGN_SYSTEM **§5** ("icon buttons `size-8`", interactive controls carry `aria-label`), and
  the criterion's literal "icon-only buttons have accessible names". Mitigation noted: both
  dialogs ship a **named** footer "Close" button, so a keyboard/SR path exists — but the
  unnamed icon control is in the tab order. Remedy is library-level (Dialog option/slot or a
  global chrome patch). The same pattern presumably affects pre-existing dialogs; cited here
  because the criterion binds every NEW surface.
- Everything else passes: limb checkboxes named "Limb 1"–"Limb 6"; all wizard/review controls
  aria-labeled (Decision subject/advisor/Decided by; Review trigger/outcome; Top-up value USD;
  Approver); keyboard focus moves into both dialogs (Tab → "Decision advisor"); visible focus
  ring on new controls (computed 3 px box-shadow ring on "Schedule review"); trajectory SVG
  `role="img"` + data-bearing aria-label ("Trajectory over 48 months: … $7.67M …"); leaver
  status carried by text badges, not colour alone.
- Out-of-scope note: the toast dismiss "X" (pre-existing chrome, 16×16, unnamed) has the same
  defect on every route — recorded for the backlog, not graded against Wave 3.

## R6.1 (corrected form) — **FAIL** (clause 1; repeat of panel 005, deepened)
- **Total minified `scaffold/dist/assets/*.js` = 1,059,074 B.** Panel 005 pinned the rubric's
  unit convention to SI (1.0 MB = 10⁶ B, from the rubric's own inline numbers); **1,059,074 B
  exceeds 10⁶ B by 59,074 B (5.9%)** — and unlike 005's marginal miss it now exceeds even a
  binary reading (1.010 MiB > 1 MiB = 1,048,576 B). Wave 3 added ~54.6 kB minified
  (005 measured 1,004,510 B). **FAIL on hard numbers, both unit readings.**
- **Initial-load payload (cold /overview, network-captured then locally gzipped):** 7 chunks —
  `index-KMTdnIAr.js` 228,512 + `Overview` 4,180 + `PoolAllocation` 1,554 + `Avatar` 1,217 +
  `PageHeader` 741 + `EmptyState` 600 + `Panel` 417 = **237,221 B gzip = 231.7 kB ≤ 290 kB** →
  clause **PASS** (20.1% headroom; better than 005's 239,351 B).

## R6.2 — PASS
First-contentful-paint per route, cold context each (preview, `performance` paint entries):
`/` 132 ms · `/overview` 132 ms · `/board` 128 ms · `/advisors` 132 ms · `/compare` 136 ms ·
`/configure` 136 ms · `/proposition` 136 ms · `/governance` 136 ms — all **< 2 s** by ~15×.

## R8.5 (corrected form) — PASS
- **(a) Deploy-ref:** `gh api repos/nordnes/comp-studio/deployments?environment=Production` →
  latest Production deployment id `5012228309`, **`ref` =
  `75ffbc7fab149e0f294105c0f169aad343a99798` = the frosty tip**, created 2026-06-10T22:43:35Z;
  its latest status **`state: "success"`**. No re-poll needed.
- **(b) Content markers** at `https://comp-studio-one.vercel.app` (HTML → referenced
  `assets/index-DtxnqpZ9.js` → its chunk graph): **"Ispahani 9 steps"** (introduced by the tip's
  most recent UI-touching commit `462e4f7`, PR #100) **PRESENT** in prod `Board-Csq9LakA.js`,
  alongside **"the Sjöström check"** and "Capital introductions"; "Model departure" PRESENT in
  prod `Advisors-B5UgsO5N.js`; the Δ9 seed names (Robert Reoch · Kerim Derhalli · Iraj Ispahani)
  PRESENT in the prod index chunk. Prod's chunking/hashes differ from the local build (682,673 B
  index vs local 781,216 B; extra `vue_vue_type_*`/core-js chunks) — exactly the false-negative
  the corrected method was written to avoid; content markers + deploy-ref both bind.
- Post-COM-34 pre-auth clause: **not yet in force** (COM-34/M6 remains open); prod HTML itself
  carries no money figure (app is client-rendered).

---

## Boundary verdict

| Item | Verdict |
|---|---|
| R2.1 | PASS |
| R2.2 | PASS |
| R2.3 | PASS |
| R2.4 | PASS |
| R1.1 (M11 slice, git-substituted) | PASS *(Linear-side Done flips unverified — MCP down)* |
| R5.1 (Wave-3) | PASS |
| R5.3 (Wave-3) | PASS |
| R2.5 (Wave-3 surfaces) | **FAIL** (CapitalRollupPanel.vue:22-25 coverage %) |
| R3.5 | PASS |
| R3.6 | PASS |
| R4.1 (Wave-3) | PASS |
| R4.2 (Wave-3) | PASS |
| R4.4 (Wave-3) | **FAIL** (Dialog-chrome close X: empty accessible name, 28 px target) |
| R6.1 | **FAIL** (total minified 1,059,074 B > 1.0 MB — repeat, deepened; initial payload 231.7 kB PASSES) |
| R6.2 | PASS |
| R8.5 | PASS (panel-005 FAIL remedied — prod now serves the tip) |

**12 of 15 graded items PASS; 3 FAIL.** All three FAILs have narrow remedies: export the
coverage ratio from `capitalRollup` and consume it (R2.5); name (or suppress) the Dialog-chrome
close X app-wide (R4.4); shed ~59 kB minified or split further off the entry (R6.1 — note the
user-paid payload is comfortably inside its bound, so the structural fix is in the total, e.g.
the 78 kB FrappeChart chunk's residency in dist or dropping dead engine surface from the index
chunk). R8.5's panel-005 failure is confirmed remedied.
