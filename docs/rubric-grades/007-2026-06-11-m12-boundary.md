# Panel 007 — M12-boundary grade (Wave 4: COM-166→170, PRs #102–#106)

- **Panel:** 007 (independent fresh-context grader, adversarial)
- **Date:** 2026-06-11
- **Tip SHA (frosty):** `1a63b50433680195d3b13f9180e1f8e087dc670a`
- **Baseline:** `3bf3e20` per RUBRIC_V2_FINISH protocol
- **Substitution note:** Linear MCP is DOWN this session — R1.1 graded on git/`gh` PR
  evidence per the panel brief, not `list_issues`.
- **Dist freshness:** `cd scaffold && npm run build` re-run before all live checks; :4173
  serves `assets/index-BIkIY2wd.js` = the fresh build's hash. Probes lived in `/tmp/claude/pw`
  only; app state driven only inside ephemeral Playwright contexts and restored in-flow.

**Result: 12 PASS · 1 FAIL (R5.1 — `VALUATION_BASES` has no by-name vector).**

---

## 1 · R2.1 + R2.2 + R2.3 — PASS

- `node engine/engine.test.mjs` → `22 passed, 0 failed.`
  `node scaffold/engine.test.mjs` → `22 passed, 0 failed.`
- Byte-identity to baseline: `git diff --quiet 3bf3e20..HEAD -- engine/engine.test.mjs
  scaffold/engine.test.mjs` → exit 0 (BYTE_IDENTICAL_TO_BASELINE).
- `node engine/engine.v2.test.mjs` → **`263 passed, 0 failed, 0 pending(v2).`**
  `grep -c 'PENDING(v2)'` over run output = 0.
- A.3 anchors via one-off import of `scaffold/src/engine.ts` (node 24, strip-types),
  `walkScenario(DEFAULT().plan,'base')`:
  bridge N 57,217.239 (±1 of 57,217) · A 75,359.291 · B 89,379.624 · C 118,707.313 ·
  bridge price $1,572.9525 (±$0.01 of $1,572.95). All PASS, exit 0.

## 2 · R2.4 — PASS

- `git log -G'SCHEMA = ' --oneline 3bf3e20.. -- scaffold/src/engine.ts` → exactly one commit,
  `1ea8432 feat(engine)!: COM-171 — the single SCHEMA-6 bump`.
- `gh api repos/nordnes/comp-studio/commits/1ea8432/pulls` → PR **#84** "COM-171 — the single
  SCHEMA-6 bump (loss-free v5→v6)", merged 2026-06-10T20:02:06Z. The commit belongs to the
  COM-171 PR and no other.
- `grep -c 'SCHEMA = 6' scaffold/src/engine.ts` = **1**.
- COM-171 round-trip vectors are inside the 263-green v2 run (R2.2).

## 3 · R1.1 (M12 slice, git-substituted) — PASS

`env -u GH_TOKEN gh pr view N --json state,mergedAt,body` for 102–106:

| PR | State | Merged (UTC) | Fixes |
|---|---|---|---|
| #102 | MERGED | 2026-06-10T23:13:45Z | COM-166 |
| #103 | MERGED | 2026-06-10T23:18:29Z | COM-167 |
| #104 | MERGED | 2026-06-10T23:27:17Z | COM-168 |
| #105 | MERGED | 2026-06-10T23:32:51Z | COM-169 |
| #106 | MERGED | 2026-06-10T23:41:37Z | COM-170 |

Fixes lines cover exactly {166,167,168,169,170}, one per PR (one issue = one PR holds).
Linear `Done` flips could NOT be verified (MCP down) — flag for the next panel with Linear up.

## 4 · R5.1 (Wave-4 engine exports) — **FAIL**

Diff base = PR #102's `baseRefOid` `15ad0ec` → HEAD on `scaffold/src/engine.ts`. Added exports:

- Type-only (exempt): `ValuationRecord` (interface), `RunbookStatus` (type).
- **Runtime:** `exerciseRunbook` (function) — covered by-name: 9 direct `ENG.exerciseRunbook(...)`
  vectors in `engine/engine.v2.test.mjs` (lines 1653–1692, T25/T26), exercising the export itself. PASS.
- **Runtime:** `VALUATION_BASES` (`scaffold/src/engine.ts:185`, `export const VALUATION_BASES =
  ['SAV','409A','SAV/409A'] as const`) — **zero by-name references in the v2 suite**
  (`grep -c 'VALUATION_BASES'` = 0; `grep 'BASES'` = 0 hits). The reconcile path consuming it
  (`engine.ts:516`) is exercised by the T25 junk-basis vector (suite line 1634, `basis:'vibes'`
  → healed to `'SAV/409A'`), but that exercises a **caller**, not the export by name — R5.1
  explicitly requires "the named assertion must exercise the export itself (not merely a caller)".

**Verdict: FAIL.** Remedy is one trivial vector, e.g.
`A('VALUATION_BASES is the closed basis vocabulary', JSON.stringify(ENG.VALUATION_BASES) === '["SAV","409A","SAV/409A"]')`.

## 5 · R5.3 (Wave-4 mirror convention) — PASS

From `gh pr view N --json files`:

- Engine-touching: **#104** (engine.ts + `engine/engine.v2.test.mjs` ✓), **#105** (engine.ts +
  `engine/engine.v2.test.mjs` ✓) — both mirrored.
- UI-only, verified genuinely engine-untouched (file lists exclude `scaffold/src/engine.ts`):
  **#102** (consents.ts, Governance.vue), **#103** (governance.ts, Advisors/Overview/Proposition.vue),
  **#106** (audit.ts, store.ts, Governance.vue). #106's store.ts additions scanned: the only
  numeric expression is a 9-step answer **count**, no money.

## 6 · R2.5 (Wave-4 surfaces, closest read) — PASS

- `consents.ts` — pure data module (matrix + facts + a filter); zero arithmetic.
- Consent-matrix render in `Governance.vue` — badges/text only; the page's only computations are
  RAG **counts** (`c[it.status]++`) and string meta-joins. The evidence-URL trust boundary
  (`/^https?:\/\//i`) is a string test, not money.
- Watermark block in `Proposition.vue` (lines 216–241) — gate is
  `grantPreconditions(sel, store.gov)` over register state; the block itself contains no
  arithmetic. (Pre-existing propText mirrors are out of Wave-4 scope and mirror engine exports.)
- Valuation form in `Configure.vue` (lines 783–843) — `setPath` writes + `fUSD` display only;
  `recordValuation` seeds the unit, no derived prices in the view.
- `ExerciseRunbook.vue` — every cost figure reads `pack.costs.*` from the engine's
  `exerciseRunbook`; max quantity from `computeGrant`. Only qty plumbing
  (`Math.round(maxQty)`, `Number(v) || 0`) — not money semantics.
- Audit section (`audit.ts` + Governance render) — timestamps, kinds, strings; the one count is
  `answers.filter(x=>x).length`.

**Zero violating lines to cite.**

## 7 · R3.8 (live, criterion-#6 flow) — PASS (both directions, restored)

On :4173, fresh Playwright context (seeded Δ9 roster):

1. `/overview` → **6** "pre-condition" chips (sample text: "4 pre-conditions").
2. `/proposition` → banner ×1 **inside `.print-area`** ("Watermarked — pre-conditions
   outstanding: A-1 Bell Rock Memorandum confirmation · A-2 Pool count / % decision (the
   Resolution 4/5 blanks) · C.6 · 10 HMRC SAV valuation · References / security checks not
   cleared…") + diagonal "Pre-conditions outstanding" ×3, `aria-hidden` decoration, also inside
   `.print-area`.
3. Greened a1/a2/open10 via the real `/governance` RAG controls + set Background check = `clear`
   via the PackageEditor combobox → banner 0 / diagonals 0 (**LIFTED**); overview chips 6→5
   (only the cleared advisor's chip drops — correct per-advisor semantics).
4. Reverse: open10 → Red → banner 1 / diagonals 3 (**RETURNS**).
5. Restore seed (a1 Amber, a2 Red, check `none`) → 1/3, matching the pre-flow state.
6. Console errors across the whole flow: **zero**.

## 8 · R3.9 (live, the runbook) — PASS

Advisors → Instruments tab → the per-option-row play button (aria-label "Exercise runbook for
this grant") on Iraj Ispahani (**UK** tax residency):

- **Blocked outside windows with the backstop date:** badge `blocked`; item: "No open exercise
  window at 2026-06-10 — exercise is restricted to Board-determined liquidity windows (**next
  backstop 2035-06-01**)."
- **s431 for a UK holder:** "UK grantee: file the s431 election within 14 days of exercise
  (restricted securities)." (status `action`).
- **Costs recompute on qty edit:** qty 858 → $1.35M · qty 10 → $16K · qty 20 → $31K
  (≈ qty × $1,572.95 strike, display-rounded). Strike/FMV line $2K/$2K (compact fUSD of
  $1,572.95), intrinsic $0 at round-derived FMV — consistent with the un-valued seed plan.
- Dialog X named "Close dialog" (installDialogA11y). Zero console errors.
- Screenshot: `/tmp/claude/pw/g007-runbook.png`.

## 9 · R4 (Wave-4 surfaces) — PASS (with one scope note)

`/governance` (full-page screenshot `/tmp/claude/pw/g007-governance-full.png`, with audit +
decision events seeded in-context so all three sections render):

- **Reading column:** `.max-w-reading` present, measured 960px. ✓
- **Section labels:** all nine headings use `.section-label` ("A · Pool & resolution" …
  "Investor consent matrix · A.6 (definitive — 47 reviewed)" · "Grant decisions · the Ispahani
  9-step artefacts" · "Audit log · append-only (2 events)"). ✓ No amber headings.
- **Amber discipline:** consent `live` badges = `Badge theme="orange"`; trigger lines =
  `text-ink-amber-strong`. DESIGN_SYSTEM §1: amber = "the current/active case" + status
  "pending/base"; `ink-amber-strong` is the sanctioned AA label amber. A live (currently-biting)
  consent right is exactly the active/pending gating case — **judged within the rule**, not
  decorative spread. Amber classes found on the page reduce to the active-Amber RAG state, the
  brand "Internal" chip, and these labels. ✓
- **Keyboard + names:** RAG segmented controls are real buttons with `aria-pressed`,
  `role=group` aria-labels ("Status — <title>"), and `focus-visible:ring-2` rings; Tab reaches
  the first status group at stop #16. Icon buttons carry aria-labels ("Remove decision
  artefact", "Edit owner, evidence and note"). Status is carried by text ("consent live", RAG
  labels, audit kind badges), not color alone. ✓
- **Dialogs:** Wave 4 added exactly **one** new dialog (ExerciseRunbook) — the brief's "two new
  dialogs" over-counts; graded ExerciseRunbook plus GrantDecisionWizard (the other dialog
  reachable from the graded surfaces). Both: `[role=dialog] button[aria-label="Close dialog"]`
  count = 1, X measured 32×32 (installDialogA11y live in the built bundle); first Tab stop in
  the wizard is the labelled "Decision subject" input. Screenshots:
  `/tmp/claude/pw/g007-runbook.png`, `/tmp/claude/pw/g007-wizard2.png`.

## 10 · R6.1 (corrected text, 2026-06-10) — PASS

- **Total minified:** Σ `scaffold/dist/assets/*.js` = **992,320 B** ≤ 1.0 MB. (16 chunks;
  index 699,838 B largest.)
- **Cold `/overview` initial JS payload** (chunks actually fetched, measured via Playwright
  network, gzip -9 of each): index 208,476 + Overview 4,312 + PageHeader 738 + PoolAllocation
  1,552 + Avatar 1,217 + Panel 419 + EmptyState 600 = **217,314 B gzip** ≤ 290 kB. ✓

## 11 · R6.2 — PASS

First-contentful-paint, cold fresh context per route: /overview 156 ms · /board 224 ·
/compare 296 · /governance 268 · /advisors 132 · /proposition 192 · /configure 128.
All < 2,000 ms.

## 12 · R8.5 (corrected method) — PASS (no re-poll needed)

- (a) `gh api repos/nordnes/comp-studio/deployments` → newest `environment:"Production"`
  deployment id 5012720992, `ref` = `1a63b50433680195d3b13f9180e1f8e087dc670a` = frosty tip;
  latest status `success` (2026-06-10T23:42:32Z).
- (b) Content markers in the **served** prod JS (chunk graph walked from prod's
  `assets/index-bOK6koi5.js`): `'Exercise runbook'` present in `Advisors-CE_g25PP.js`;
  `'append-only'` present in `Governance-BBhX9XDY.js`. (Vercel's chunking differs from the
  local build, as panel 005 documented — markers, not hashes.)

## 13 · R7.1 (Wave-4 slice) — PASS

`memory.md` carries dated 2026-06-11 entries, each naming its PR:
COM-166/PR #102 (line 2650) · COM-167/PR #103 (2659) · COM-168/PR #104 (2668) ·
COM-169/PR #105 (2678) · COM-170/PR #106 + the M12-complete star entry (2688–2691).

---

## Verdict

**12 / 13 PASS. One FAIL: R5.1** — the Wave-4 runtime export `VALUATION_BASES`
(`scaffold/src/engine.ts:185`) has no by-name vector in `engine/engine.v2.test.mjs`; the rubric
requires every new value export to be exercised by name, not via a caller. One-line vector fixes
it. Everything else at the M12 boundary holds: suites green and baseline-identical, the single
COM-171 SCHEMA bump, all five Wave-4 PRs merged with exact Fixes coverage, the mirror convention,
zero unsanctioned money math on the new surfaces, the criterion-#6 watermark flow live in both
directions, the runbook's window/s431/cost behaviour, design-system conformance incl. the named
dialog X, 992,320 B total / 217,314 B initial gzip, sub-300 ms paints, prod serving the tip with
Wave-4 markers, and memory.md fully logged. R1.1's Linear `Done` flips remain unverified (MCP
down) — re-check when Linear returns.
