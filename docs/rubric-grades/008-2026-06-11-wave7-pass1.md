# Rubric panel 008 — 2026-06-11 · Wave-7 perfection-loop pass #1 (retro-committed)

Four fresh-context graders over the full rubric (workflow wf_81d98bf4-428; preview at the
then-tip e1838bf). Committed retroactively — the verdict synthesis ran but the doc-commit
step was interrupted by the Linear/spend outage window.

**Totals: 38 PASS · 10 FAIL · 6 BLOCKED-EXTERNAL of 54 graded.**

## FAILs (all addressed in PRs #110–#115 + the R7.2/R7.3 doc refreshes)

- **R1.2 — legacy queue resolved (COM-28/21/31/61/87 + epics COM-5/7), three lowest verified** (G1): Three lowest-numbered = COM-5, COM-7, COM-21. COM-21: verified — docs/legacy-audit-w5.md (CLOSEABLE, 24/25 live probes) and its cited files check out (FrappeChart.vue:89 role=img/aria-label; style.css:80-83 .range-input:focus-visible). COM-28/31 residuals shipped (PRs #107 d811207, #108 55aad6b merged). COM-87: Duplicate-of-143 with tombstone (memory.md:2287-2289). BUT COM-5 and COM-7 have ZERO re
- **R5.1 — every NEW engine runtime export has a by-name vector in the v2 suite** (G1): Diffed value exports 3bf3e20→HEAD (~60 added); loop-grep for ENG.<name> in engine/engine.v2.test.mjs: 59/60 referenced by name, ONE MISS — preTgeLiquidity (engine.ts:939, COM-152 runtime export). 'ENG.preTgeLiquidity' appears nowhere in the suite; T13 (lines 793-835) sets the raw scenario FIELD (plan.scenarios.base.preTgeLiquidity) and asserts via CALLERS (tokenValueFor/computeAdvisor/computeBoard
- **R7.2 — CLAUDE.md live-prompt pointer + engine-gate clause reflect the END state** (G1): CLAUDE.md last touched at 2d3b06c (run start, pre-baseline; git log -- CLAUDE.md). The engine-gate clause is still accurate (RFC §7 unfreeze, scaffold/src/engine.ts live), but the pointer still reads 'authorized by Robin 2026-06-10 — "Nothing is blocked"' while Wave 6 is now BLOCKED on recorded externals (memory.md final entry), and nothing reflects the run's end state (M10/M11/M12 complete; M6 hu
- **R7.3 — auto-memory MEMORY.md updated at the final gate with the run outcome** (G1): /Users/nordnes/.claude/projects/-Users-nordnes-dev-comp-studio/memory/MEMORY.md mtime 2026-06-10 11:17 — BEFORE the run's first wave shipped; content still describes the armed pre-run state ('the live run pair @ ae8f035'). No run-outcome (or even mid-run) update exists; updating this file requires no external service, so the Linear/Supabase blocks do not excuse it.
- **R8.4 — verdict trail committed; LAST TWO full panels zero FAILs; rubric history tighten-only** (G1): Trail exists and is committed: docs/rubric-grades/001–007 all in 3bf3e20..HEAD. Rubric history (4 commits: ae8f035, 17d1d1d, bc907f8, 212529f) shows tightening + two corrections documented under the protocol's rubric-bug clause with original numbers preserved (R6.1, R8.5 — graded on current text per the brief); no waiver entries exist. BUT the last two panels are 006 (3 FAILs: R2.5/R4.4/R6.1) and 
- **R3.17 — SAV/409A valuation entered once → the identical figure appears on every surface that shows it (COM-168)** (G3): Probes /tmp/claude/pw/G3/r317.mjs + r317b.mjs: entered $1,650 once in Configure (stored exactly: plan.valuation {ppsUSD:1650, basis:'SAV/409A', dateISO:'2026-06-11'}); all surfaces do read the single record and update together (single-source behaviour holds). But the FIGURE SHOWN is not identical across the enumerated surfaces because fUSD (engine.ts:214-219) collapses any pps in $1,500–$2,499 to 
- **R3.18 — Audit log appends entries for: a grant change, a consent flip, a valuation edit (COM-170)** (G3): Probe /tmp/claude/pw/G3/r318.mjs: (1) grant change — added an option grant to Iraj → audit appended 'grant: Grant added: option at seriesC (draft)' ✓; (2) valuation edit — recorded $1,700 → 'valuation: Valuation recorded: $1700/share (SAV/409A, 2026-06-11)' ✓; (3) consent flip — flipped B-3 'Pantera consent — per executive-officer grant' red→green on /governance (aria-pressed=true; persisted gover
- **R4.2 — amber ONLY as current/active-case + status semantics (grep template classes + screenshot sweep)** (G4): The tier pill uses the amber accent for tier CLASSIFICATION: scaffold/src/components/roster/TierBadge.vue:15 (`<Badge :label="label" theme="orange" variant="subtle" size="sm" />` — 'Anchor'/'Strategic'/'Base'/'$value') and scaffold/src/views/Compare.vue:316-319 (tier Badge theme="orange"). Live computed colors on /board (probe r4-live.mjs): bg rgb(253,250,237)=#FDFAED (surface-amber-1), ink rgb(13
- **R4.4 — WCAG spot-grade on every NEW surface: keyboard-reachable controls with visible focus ring; icon-only buttons named; non-color status channel; chart text alternatives** (G4): FAIL on the visible-focus-ring clause: keyboard-focused frappe-ui Select triggers render NO visible focus indication. Hard evidence (r4-deep2/3/4.mjs): GrantDecisionWizard's 'Decision advisor' combobox (NEW surface, COM-165) when Tab-focused has computed box-shadow 'rgba(0,0,0,0) 0px 0px 0px 0px, rgba(0,0,0,0) 0px 0px 0px 0px, rgba(0,0,0,0) 0px 0px 0px 0px' + outline 'rgba(0,0,0,0) solid 2px' — al
- **R6.3 — stress fixture at the pinned path scaffold/fixtures/board-25x4.mjs (loadable via console or query param); 25×4 board keeps Board/Compare/Overview interactive, keystroke-to-DOM < 100 ms** (G4): The pinned fixture does not exist: /Users/nordnes/dev/comp-studio/scaffold/fixtures/ → 'No such file or directory'; repo-wide `find . -name "*25x4*"` (excluding node_modules) = zero hits; grep for 'board-25x4|25x4|stress' across scaffold/, docs/, memory.md = zero hits (the only 'fixture' mentions are the unrelated DEFAULT() test-fixture comments at store.ts:140 / engine.ts:367); no query-param loa

## BLOCKED-EXTERNAL (Supabase org invoices + the Linear MCP outage)

- **R1.4 — COM-33/36/71 carry current-status comments dated during the run; still open** (G1): Linear MCP down — comments can neither be posted nor read (the outage is a listed external). Substitute: /tmp/claude/linear-debt.md (mtime 2026-06-11 00:46, during the run) holds the section 'COM-33/36/71 status comments (leave open)' with current st
- **R1.6 — M6 pair COM-34 (auth) + COM-35 (persistence) Done** (G1): Not Done — blocked on the externals named in the panel brief: Supabase project creation hit PaymentRequiredException ('overdue invoices in the organization Raiku' — settling is human-only) and the Linear outage withholds the L-sized specs (security r
- **R5.6 — RLS proof for COM-35 tables** (G1): No COM-35 tables exist to prove: zero .sql files and no supabase/ dir in the repo (find sweep clean). COM-35 is blocked on the external named in the brief — Supabase PaymentRequiredException (overdue Raiku org invoices, human-only) + the Linear outag
- **R8.1 — Linear project status update with PR ranges + final panel grade summary** (G1): Posting/reading a Linear project status update is impossible — the Linear MCP outage is a listed external (substitution reported per the brief). No repo-side drafted status update with PR ranges + per-section grade summary exists either (the FINAL pa
- **R8.2 — main re-synced to the finished frosty state, or the COM-36 'ready to cut' comment** (G1): Arm 1: origin/main = 93d6073 ≠ frosty tip e1838bf — not re-synced; the 'finished frosty state' cannot exist while Wave 6 (COM-34/35) is blocked on the Supabase-invoice external. Arm 2: the one-line 'sync is ready to cut' comment on COM-36 can neither
- **R3.10 — persistence + confidentiality UNCONDITIONAL: state survives reload AND fresh context behind login; pre-auth DOM confidentiality sweep; documented test users** (G2): Split verdict per the run note (COM-34/35 blocked on the overdue Supabase org invoices + the Linear outage; memory.md:2722 records 'status comments on COM-34/35 (blocked: Supabase org invoices, human-only)'). PERSISTENCE HALF — PASS on live probe /tm

## Grader summaries

- **G1**: Panel 008 (G1, Wave-7 FULL pass, sections R1/R2/R5/R7/R8 = 25 criteria) at frosty tip e1838bf, preview verified serving the pinned index-D4CYnukt.js: 15 PASS, 5 FAIL, 5 BLOCKED-EXTERNAL. Engine integrity is fully green (R2.1–R2.5: 22/22 ×2 byte-stable anchors, v2 264/0/0 with zero pendings, live-engine A.3 anchors inside tolerance, single COM-171 SCHEMA-6 commit with both v5→v6 paths vectored, no unsanctioned view money math). Testing depth nearly holds (store suite wired+green, mirror conventio
- **G2**: G2 (panel 008, Wave-7 full-rubric pass) graded R3.1–R3.10 as ten separate live end-to-end Playwright probes against the pinned preview at http://[::1]:4173 (served bundle index-D4CYnukt.js verified at start, mid-run, and end — matches the e1838bf build; no rebuild performed). Result: 9 PASS, 1 BLOCKED-EXTERNAL (R3.10 — its persistence half passes in full: localStorage round-trip across reload is byte-identical, the #s= hash boots a carried board in a virgin context, and JSON export→import round-
- **G3**: Panel 008 / G3 — R3.11–R3.19 graded as live end-to-end probes on http://[::1]:4173 (served index-D4CYnukt.js verified to match tip e1838bf; per my brief I did NOT rebuild — this substitutes for the rubric's dist-freshness rebuild clause, hash-verified instead). Linear MCP down: issue ids corroborated via source comments/git per the substitution rule. Repo untouched (probes live in /tmp/claude/pw/G3; the stray scaffold/.agents/ + skills-lock.json untracked entries pre-date my run and were not wri
- **G4**: Panel 008 (Wave-7 FULL pass), grader G4 — R4 + R6, all 10 criteria graded against the preview of the built dist at tip e1838bf (served index-D4CYnukt.js hash-verified at session start and end; rebuild substituted by hash-match per the shared-preview instruction; Linear MCP down but no R4/R6 criterion needed it; repo untouched — all probes/screenshots in /tmp/claude/pw/G4/). Result: 8 PASS, 2 FAIL, 0 BLOCKED-EXTERNAL. PASSES: R4.1 (layout grammar holds on all 7 routes — widths, section labels, fi
