# Rubric panel 009 — 2026-06-11 · Wave-7 perfection-loop pass #2 (post-UXS sweep #115–#134)

Four fresh-context graders (workflow wf_c0fea6ad-dee) at tip 57476c8, preview hash-verified
serving index-BFn0iq3m.js. Flows fully green: G2 9/9 (R3.1–10) + G3 9/9 (R3.11–19) with ZERO
console errors across all probe runs; engine integrity fully green (22/22 ×2, v2 283/0/0,
anchors in tolerance, single SCHEMA commit). The FAILs are operational/perf only.

**Totals: 40 PASS · 8 FAIL · 6 BLOCKED-EXTERNAL of 54 graded.**

## FAILs (→ FIX-9)

- **R5.1 — every NEW engine runtime export has a by-name vector in engine/engine.v2.test.mjs** (G1): Full-population check (not a sample): 71 value exports added since 3bf3e20 (git diff '+export const|function' parse), looped word-boundary grep against engine/engine.v2.test.mjs → 69/71 referenced, TWO MISSES: fPps (scaffold/src/engine.ts:245) and fDateDay (scaffold/src/engine.ts:256). `grep -n 'fPps\|fDateDay' engine/engine.v2.test.mjs scaffold/store.test.mjs scaffold/engine.test.mjs` → zero hits in any suite. Both are runtime value exports (per
- **R5.3 — mirror convention: the three highest-numbered merged PRs touching scaffold/src/engine.ts also touched the spec suite** (G1): The actual top three (mapped via git merge ancestry, not the briefed candidates): #132 (a82e692, UXS-L), #128 (45c2c09, UXS-H), #122 (572626b, UXS-C). `gh pr view N --json files`: #122 touched engine/engine.v2.test.mjs ✓, but #132 and #128 list NO test file at all (engine.ts + views/governance/store only) — two misses in the pinned sample. Per the one-miss rule I audited ALL engine-touching merged PRs since baseline (git diff m^1..m per merge): 3
- **R7.2 — CLAUDE.md live-prompt pointer + engine-gate clause reflect the END state** (G1): CLAUDE.md last committed 709c057 (2026-06-11 02:07, the panel-008 R7.2 fix) — the run-state line still reads 'M10 + M11 + M12 SHIPPED (PRs #68–#110; v2 suite 22→265 vectors…)' (CLAUDE.md:81-83) while the graded state is PRs #68–#134 merged, v2 suite at 283, the M13/UXS final sweep complete, the UXP P2 tail + Linear issue-creation debt (task #13) open. The engine-gate clause itself is still accurate (RFC §7, scaffold/src/engine.ts live) and the hu
- **R7.3 — auto-memory MEMORY.md updated at the final gate with the run outcome** (G1): /Users/nordnes/.claude/projects/-Users-nordnes-dev-comp-studio/memory/MEMORY.md: 794 B, mtime 2026-06-11 02:07 — content reads 'perfection loop + final sweep live — PRs #68–#110, v2 suite 265 vectors, Linear all flipped'. The sweep has since COMPLETED (#115–#134; v2 at 283; new Linear debt outstanding, COM-176 + UXS issues) and no final-gate run-outcome update exists. No external service is needed to update this file, so the outage does not excus
- **R8.3 — clean tip: git status clean; no .only/.skip/xfail; no fixture secrets; churn files match origin** (G1): `git status --porcelain` at tip 8479542 → TWO untracked files: research/COMPETITIVE_LANDSCAPE.md and research/UI_LIBRARY_LANDSCAPE.md (final-sweep research work products; the latter is referenced by the committed UX_IMPROVEMENT_PLAN.md) — the tree is not clean. Everything else holds: OBJECTIVES_AND_FLOWS.md resolved per the Wave-0 decision (deleted; ULTRACODE_V2_FINISH.md:132 prescribes exactly that), zero .only/.skip/xfail across engine/*.mjs + 
- **R8.4 — verdict trail committed; LAST TWO full panels zero FAILs; rubric history tighten-only** (G1): Two of three clauses hold: `git ls-files docs/rubric-grades/` → all eight panels 001–008 committed (008 retro-committed at 8479542 with its outage explanation); `git log --oneline -- RUBRIC_V2_FINISH.md` → 4 commits (ae8f035 authoring + 17d1d1d/bc907f8/212529f), all tightenings or protocol-documented rubric-bug corrections with original numbers preserved (R6.1, R8.5 — graded on current text), zero waiver entries. FAIL on the exit clause: the last
- **R6.1 — total dist JS ≤ 1.0 MB minified AND cold-/overview initial payload ≤ 290 kB gzip (exact numbers)** (G4): CLAUSE 1 FAILS: Σ scaffold/dist/assets/*.js = 1,037,923 bytes minified (17 chunks; index-BFn0iq3m.js alone 714,293 B) > 1.0 MB under the rubric's own established decimal convention — panel 005 FAILed this clause at '1,004,510 B > 10⁶ B', panel 006 FAILed at 1,059,074 B, panel 007 PASSed at 992,320 B, panel 008 passed at tip e1838bf; the 20-PR UXS sweep (#115–#134) regrew the bundle 37,923 B (3.8%) past the line. (Under MiB it would squeak by at 0
- **R6.3 — stress fixture at the pinned path scaffold/fixtures/board-25x4.mjs (loadable via console or query param); 25×4 board keeps Board/Compare/Overview interactive, keystroke-to-DOM < 100 ms** (G4): The pinned fixture still does not exist at tip 57476c8: /Users/nordnes/dev/comp-studio/scaffold/fixtures/ → 'No such file or directory'; find . -name '*25x4*' (excl. node_modules) = zero hits; the only '25x4' string in the repo is panel 008's own FAIL record (docs/rubric-grades/008:20). No console/query-param loader exists either. This is a REPEAT of panel 008's FAIL, unaddressed by PRs #110–#134 despite panel 008's header claiming 'FAILs (all ad

## BLOCKED-EXTERNAL (Supabase Raiku-org invoices + the Linear MCP outage)

- **R1.4 — COM-33/36/71 carry current-status comments dated during the run; still open** (G1): The Linear MCP outage (briefed external) blocks both posting and reading. Substitute evidence: /tmp/claude/linear-debt.md §'COM-33/36/71 status comments (leave open)' holds the drafted current-status text dated during the run; memory.md:2721-2722 rec
- **R1.6 — M6 pair COM-34 (auth) + COM-35 (server persistence) Done** (G1): Not Done, per the briefed external: Supabase project creation fails with PaymentRequiredException — 'overdue invoices in the organization Raiku' (settling is human-only), and the Linear outage withholds the L-sized issue specs that carry mandatory se
- **R5.6 — RLS proof for COM-35 tables (migrations + live cross-user probe + advisor lints)** (G1): No COM-35 tables exist to prove: `find . -name '*.sql'` (excl. node_modules) → zero files; no supabase/ directory. COM-35 is blocked on the briefed external — Supabase PaymentRequiredException 'overdue invoices in the organization Raiku' (human-only)
- **R8.1 — Linear project status update with PR ranges + final panel per-section grade summary** (G1): Posting/reading a Linear project status update is impossible — the MCP outage is the briefed external (substitution reported). No repo-side or /tmp/claude draft of a status update with PR ranges + per-section grade summary exists either (grep across 
- **R8.2 — main re-synced to the finished frosty state, or the COM-36 'sync is ready to cut' comment** (G1): Arm 1 false on hard evidence: origin/main = 93d6073 (the M9 sync) ≠ frosty tip 8479542 — and the 'finished frosty state' cannot yet exist while Wave 6 (COM-34/35) is blocked on the Supabase-invoice external and the loop is mid-run. Arm 2 (whether Rob
- **R3.10 — Persistence + confidentiality, UNCONDITIONAL: full state survives reload AND a fresh browser context behind login; pre-auth DOM confidentiality sweep; seeded test users documented** (G2): SPLIT per the run note. PERSISTENCE HALF — PASS in full (probes /tmp/claude/pw/G2-panel009/r310.mjs + r310b.mjs): (a) after mutating every slice family (advisor 'Persist Probe', governance A-1→green, an M11 review, scenario set 'P10 set', audit entri

## Grader summaries

- **G1**: Panel 009 / G1 — Wave-7 FULL-rubric pass #2 (post-UXS-sweep #115–#134), sections R1+R2+R5+R7+R8 = 25 criteria, graded adversarially at frosty tip 8479542 (HEAD advanced one docs-only commit past the briefed 57476c8 mid-panel; the :4173 preview verified serving the pinned index-BFn0iq3m.js — no app code changed after 57476c8, so no rebuild was needed or performed). Result: 14 PASS · 6 FAIL · 5 BLOCKED-EXTERNAL. Engine integrity is fully green (R2.1–R2.5: 22/22 ×2, v2 283/0/0 with zero pendings, live-import anchors asserted in tolerance, single COM-171 SCHEMA commit via PR #84 with both v5→v6 paths vectored, no unsanctioned view money math). Prod matches the tip by the corrected two-check method (R8.5). Trust boundaries held under live attack (R5.4). The six FAILs: R5.1 — fPps (engine.ts:245) and fDateDay (engine.ts:256) shipped with zero by-name vectors in the v2 suite (a recurrence of the panel-004/007/008 export-coverage class, introduced by the post-008 fixes themselves); R5.3 — the top three engine-touching PRs are #132/#128/#122 and two of them (#132, #128 — both 1-line copy-string engine edits) touched no spec-suite file (full audit: 31/33 mirrored); R7.2/R7.3 — CLAUDE.md and the auto-memory index were last refreshed 02:07, before the entire 20-PR sweep (still read 'PRs #68–#110 / 265 vectors / sweep live'); R8.3 — two untracked research files (COMPETITIVE_LANDSCAPE.md, UI_LIBRARY_LANDSCAPE.md); R8.4 — the last two committed panels (007: 1 FAIL, 008: 10 FAILs) are not zero-FAIL, so the loop's exit condition is unmet (by design mid-run). BLOCKED-EXTERNAL (5): R1.4/R8.1/R8.2 on the Linear MCP outage (substitutions reported — drafted/applied comments evidenced via /tmp/claude/linear-debt.md + memory.md:2715-2722; origin/main remains at 93d6073) and R1.6/R5.6 on the overdue Raiku Supabase org invoices (human-only; no COM-35 SQL exists to lint). Linear-named criteria R1.1/R1.2/R1.3 PASS via the briefed git/PR substitution (all M10–M12 flips were applied during the documented reconnection window; COM-21's cited artifacts re-verified at FrappeChart.vue:89 and style.css:80-83). Operational notes for the orchestrator: the briefed R5.3 candidates (#119/#122/#131) were wrong — the actual top three engine-touchers are #132/#128/#122; /tmp/claude/pw/G1 already belonged to panel 008's G1 (zsh noclobber caught the collision), so my probes live in /tmp/claude/pw/G1-009/; the zsh-heredoc-mangles-! rule bit twice and was routed through Write-files per the memory rule; a Vercel-plugin hook demanded the 'verification' skill on my vite-build probe — declined as out-of-scope for a read-only grader. Repo untouched; verdict delivered here only.
- **G2**: G2 (panel 009, Wave-7 FULL-rubric pass #2 after PRs #115–#134) graded R3.1–R3.10 as ten separate live end-to-end Playwright probes against the shared preview at http://[::1]:4173. Dist freshness: the served bundle was hash-verified as index-BFn0iq3m.js at session start, in every probe, and at session end — it matches the pinned 57476c8 build (no rebuild performed per the shared-preview instruction; note the repo tip advanced mid-run to 8479542, a docs-only commit touching no scaffold/src file, so the preview still corresponds to the live app code). Result: 9 PASS, 1 BLOCKED-EXTERNAL (R3.10 — its persistence half passes in full: byte-identical localStorage across reload covering board+governance+audit+M11 slices, the #s= hash boots a carried board in a virgin context, JSON export→import round-trips; the behind-login/pre-auth-DOM/test-user-docs clauses are COM-34/35-dependent and those are blocked on the overdue Supabase Raiku-org invoices, a recorded human-only external). Notable probe adaptations vs panel 008: NumIn is now click-to-edit (button 'Label: $X' → input), the review top-up now freezes an explicit count at approval (UXS-A) so the literal $value/(FMV−strike) leg of R3.1 was driven via the app's own Import JSON path instead, and /board 'Add' now opens the editor directly (UXS-L). The Overview chip in R3.7 required row-scoping — the six seeded advisors legitimately keep '1 pre-condition' (uncleared background checks) after the B-3 flip; the exec advisor's chip clears, which is the correct gating semantics. CONSOLE-ERROR REPORT for G3 (R3.19): ZERO console errors and zero pageerrors were observed across all 14 probe runs, including the two virgin contexts (every probe captured console level:error). Linear MCP down per the brief: no R3.1–R3.10 criterion needed Linear; COM-issue ids were corroborated via source-comment markers (e.g. COM-144/149/157/161/166/169 annotations in scaffold/src). Repo untouched — all probe scripts, state dumps, and screenshots live in /tmp/claude/pw/G2-panel009/. The budget banner (board tokens 4.83% > 4.50% bucket) renders on seeded boards — by design, reported for context only.
- **G3**: Panel 009, grader G3: R3.11–R3.19 ALL PASS (9/9) via independent end-to-end Playwright probes against the :4173 preview (served bundle verified = index-BFn0iq3m.js, tip 57476c8; no rebuild). Every money assertion was cross-checked against node-side imports of the live engine (scaffold/src/engine.ts) — round-close re-pricing ($1,572.95→$1,592.37, +50% crystallised uplift, $5.11M→$7.67M), pre-TGE re-statement ($5.40M→$2.17M), the 2× cash-floor trade ($200K) and affordability breach ($2K/mo > 10% × $20K), guardrail compa/ceiling figures, and the SAV/409A $1,650.00 across six enumerated surfaces. All flows left zero console errors and restored localStorage; probe scripts live in /tmp/claude/pw/G3/p009/ (repo untouched). Substitutions per briefing: Playwright listeners for preview_console_logs; no Linear checks were required by these criteria. Probe-side false starts (tab role, $5.00M regex, init-script re-seeding) were diagnosed as grader artifacts and corrected before verdict — no app defects found in scope.
- **G4**: Panel 009 (Wave-7 FULL pass #2, post #115–#134 sweep), grader G4 — R4 (all 7) + R6 (all 3) graded adversarially against the shared preview at http://[::1]:4173, hash-verified serving index-BFn0iq3m.js (= the dist built at tip 57476c8) at session start and end; rebuild substituted by hash-match per the panel brief (reported as a deviation from the rubric's dist-freshness rebuild clause). Repo untouched; all probes/screenshots in /tmp/claude/pw/G4-009/. Linear MCP not needed for these sections; no Wave-6 (COM-34/35) dependency, so no BLOCKED-EXTERNAL verdicts. Result: 8 PASS, 2 FAIL. Both pass-1 R4 FAILs are verified fixed (TierBadge gray; the combobox focus ring renders — note the 150ms box-shadow transition that makes a synchronous computed-style read lie). The axe floor from PR #127 holds (0 critical / 0 serious color-contrast on /overview+/board). FAIL 1 — R6.1 clause 1: total dist JS regrew to 1,037,923 B > 10⁶ B (the convention three prior panels enforced), over by 3.8% after the UXS sweep; the initial-payload clause passes comfortably at 225,050 B gzip (225.1 kB ≤ 290 kB). FAIL 2 — R6.3, a straight repeat: scaffold/fixtures/board-25x4.mjs still does not exist (panel 008's header claim that all its FAILs were addressed is wrong for this one); interactivity itself is excellent on a synthetic 25×4 board (max 13.3 ms < 100 ms), so the remedy is just committing the pinned fixture + loader and shedding ~38 kB off the entry bundle.
