# Rubric panel 010 — 2026-06-11 · Wave-7 perfection-loop pass #3 (post-FIX-9)

Four fresh-context graders (workflow wf_0a1c27ff-0c4), preview hash-verified index-P_vMgIRH.js.
**G2 + G3 + G4 fully green (28 PASS, zero FAILs):** all 19 flows, zero console errors across
20+ probe runs with grader-independent arithmetic; axe 0 critical + 0 contrast-serious; total
JS 969,733 B ≤ 1.0 MB; payload ~200–205 kB gzip ≤ 290; the 25×4 fixture at ≤25.1 ms.
Both of panel 009's perf FAILs verified fixed to the byte.

**Totals: 45 PASS · 5 FAIL · 1 exit-checker · 3 BLOCKED-EXTERNAL of 54 graded.**

## FAILs (G1 operational — → FIX-10 + the R5.3 waiver decision)

- **R1.2 — legacy queue (COM-28/21/31/61/87 + epics COM-5/7) closed with comments citing a PR number AND a file path; verify three lowest** (G1): Two of seven closing comments cite NO PR number: COM-21 (comment c0ec7908, 2026-06-11T00:36 — cites docs/legacy-audit-w5.md + FrappeChart.vue:89 but no PR) and COM-61 (comment dd05850d — cites Configure.vue:179-183, COM-95/76/91, no PR). The criterion's first clause requires a PR number AND a file path in each. One miss expanded the audit to all seven: COM-5→PR #107 ✓, COM-7→PR #108 ✓, COM-28→#107 ✓, COM-31→#108 ✓, COM-87→#73 ✓ — all four cited P
- **R5.3 — mirror convention: the three highest-numbered merged PRs touching scaffold/src/engine.ts also touched the spec suite** (G1): Recomputed across the whole run: the top three engine-touching merged PRs are #132 (a82e692, UXS-L), #128 (45c2c09, UXS-H), #122 (572626b, UXS-C). gh pr view --json files: #122 touched engine/engine.v2.test.mjs ✓, but #132 and #128 list NO spec-suite file (engine.ts + views/governance/store only) — two misses in the pinned sample. One-miss expansion audited the recent population: #113/#115/#116/#117/#119/#122 all mirrored ✓. The misses are the tw
- **R8.1 — Linear project status update describes the run outcome with PR ranges + the FINAL panel's per-section grades, health honest** (G1): get_status_updates(project) → the most recent update is 'M9 · UX/UI v2 — GATED (2026-06-10)' (created 2026-06-10T02:41, edited 09:02, health onTrack). No status update covering the V2-finish run outcome (M10–M13, PRs #68–#135, panel grade summary) exists. A final-gate deliverable that cannot exist before the loop exits — expected to FAIL mid-run, but it factually fails today; the Linear MCP is live so nothing external blocks posting it at the gat
- **R8.2 — main re-synced to the finished frosty state, OR a 'sync is ready to cut' comment on COM-36** (G1): origin/main = 93d6073 (the M9-state sync), now 189 commits behind the frosty tip 293bcde — not the finished state. The fallback also fails: COM-36's complete comment list (2 comments, hasNextPage false; latest 2026-06-10T08:45) contains no 'sync is ready to cut' line. Neither branch of the criterion holds. Remedy at the gate: either cut the sync (pattern of 93d6073, QA green at merge) or post the one-line ready-to-cut comment on COM-36.
- **R8.3 — clean tip; no .only/.skip/xfail; no fixture secrets; churn files match origin** (G1): git status at tip 293bcde → untracked 'prototype/' (the 9-page hi-fi HTML prototype + assets, 104K; memory.md:2879 documents it as deliberately built for M14 Phase 3 but never committed) — the tree is not clean. The panel-009 offenders (research/*.md) WERE resolved (committed in PR #135) and everything else holds: zero .only/.skip/xfail across engine/*.mjs + scaffold/*.mjs; scaffold/fixtures/board-25x4.mjs is deterministic synthetic data, zero pa

## Exit checker

- **R8.4 — verdict trail committed; LAST TWO full panels zero FAILs; rubric history tighten-only with protocol-compliant waivers** (G1): Exit checker — pending two clean panels. Factual state: the last two full panels carry FAILs (008: 38 PASS/10 FAIL/6 BLOCKED; 009: 40/8/6). The trail itself is HONEST on independent re-derivation: docs/rubric-grades/ has all nine panels 001–009 committed; panel 009's recorded evidence matches my own

## BLOCKED-EXTERNAL (Supabase Raiku-org invoices)

- **R1.6 — M6 build pair COM-34 (auth) + COM-35 (server persistence) Done, with exactly COM-33/36/71 exempt** (G1): COM-34/35 are Backlog, not Done — blocked on a documented external: Supabase project creation fails with PaymentRequiredException ('overdue invoices in the organization Raiku', human-only to settle), per status comments dated 2026-06-11 on both issue
- **R5.6 — RLS proof for COM-35 tables (migration SQL + live two-user probe + Supabase advisors lint)** (G1): No COM-35 tables, migrations, or Supabase project exist to grade: project creation fails on the overdue Raiku-org invoices (PaymentRequiredException, human-only), per the 2026-06-11 status comments on COM-34/35. The pre-COM-34 obligation on record — 
- **R3.10 — Persistence + confidentiality, UNCONDITIONAL: full state survives reload AND a fresh browser context behind login; pre-auth DOM confidentiality sweep; seeded test users documented** (G2): SPLIT per the run note. PERSISTENCE HALF — PASS in full (probe /tmp/claude/pw/G2010/r310.mjs, 10/10 checks): after mutating every slice family (advisor 'Persist Probe', governance A-1 amber→green, an M11 scheduled review, scenario set 'P10 set', audi

## Grader summaries

- **G1**: Panel 010 (Wave-7 FULL pass #3, post-FIX-9), grader G1, sections R1+R2+R5+R7+R8 = 25 criteria at frosty tip 293bcde (HEAD advanced one docs-only memory commit mid-panel past the briefed b3e2bee; the :4173 preview verified serving index-P_vMgIRH.js = dist/index.html, and my own temp-outDir build of the tip reproduced the same hash — no rebuild of the shared dist performed). Result: 17 PASS · 6 FAIL · 2 BLOCKED-EXTERNAL. Engine integrity is fully green (R2.1–R2.5: 22/22 ×2, v2 287/0/0 zero pendings, live-import anchors in tolerance, single COM-171 SCHEMA commit via PR #84 with both v5→v6 paths vectored, no unsanctioned view money math). FIX-9 verified effective on its claims I grade: R5.1 (70/70 new value exports by-name, fPps/fDateDay T31 pins exercise the exports directly), R7.2/R7.3 (CLAUDE.md + auto-memory state the post-sweep truth), R8.5 (prod = tip by deploy-ref success + the dc198f6 '25x4' marker in prod JS), and the panel-009 R8.3 research files are committed. Trust boundaries held under live attack (R5.4: junk hash, injected javascript: URLs stripped at reconcile, junk JSON refused, 3.2MB junk CSV refused; storage restored). The 6 FAILs: R1.2 — COM-21 and COM-61 closing comments cite file paths but no PR number (substance verified green; a comment-format breach, fixable by appending citations or a docs: rubric correction); R5.3 — REPEAT and structurally uncloseable as written: the top three engine-touching PRs remain #132/#128/#122 and #132/#128 touched no spec-suite file (post-hoc T31 mirrors in #135 don't satisfy 'the same PR'; merged history can't be retro-mirrored — needs a Robin waiver or a docs: criterion correction, not another fix PR); R8.1 — the latest Linear status update is still the 2026-06-10 M9 gate (the final-gate update doesn't exist yet; MCP is live, nothing blocks posting it at exit); R8.2 — origin/main sits 189 commits back at 93d6073 AND COM-36 has no 'sync is ready to cut' comment (one line closes this); R8.3 — NEW untracked dir prototype/ (the M14 hi-fi prototype, documented in memory.md but uncommitted) dirties the tree, everything else clean; R8.4 — exit checker: pending two clean panels (008: 10F, 009: 8F; trail independently verified honest — panel 009's evidence matches my re-derivation exactly; rubric history tighten-only, zero waivers). BLOCKED-EXTERNAL (2): R1.6 + R5.6 on the overdue Raiku Supabase invoices (human-only; pre-COM-34 obligations met — both issues carry dated blocked-status comments and the staged plan). Orchestrator notes: of the six FAILs, three are one-action items (R8.1 status update, R8.2 comment, R8.3 commit-or-ignore prototype/), one is comment-editing (R1.2), and R5.3 cannot be cleared by any code PR — it needs the waiver-or-rubric-correction decision before any panel can go zero-FAIL; R8.4 stays FAIL by construction until two clean panels exist. Probes live in /tmp/claude/pw/G1010/; repo untouched; never ran vp check --fix; a Vercel-plugin hook demanded a Next.js skill on my vite build — declined as a false-positive pattern match in a Vue/Vite repo.
- **G2**: G2 (panel 010, Wave-7 FULL-rubric pass #3 after FIX-9 / PR #135) graded R3.1–R3.10 as separate live end-to-end Playwright probes against the shared preview at http://[::1]:4173. Dist freshness: the served bundle was hash-verified as index-P_vMgIRH.js at session start, inside every probe (servedHash step), and at session end — it matches scaffold/dist/index.html; no rebuild performed per the shared-preview instruction. Result: 9 PASS, 1 BLOCKED-EXTERNAL (R3.10 — its persistence half passes in full: byte-identical localStorage across reload covering board+governance+audit+M11 slices, the #s= hash boots a carried board in a virgin context, JSON export→import round-trips; the behind-login/pre-auth-DOM/test-user-doc clauses are COM-34/35-dependent, blocked on the overdue Raiku Supabase invoices). Probes carry grader-independent arithmetic, not app echoes: R3.1's 2,575 = 50000/(1592.371668909606−1572.9525022155865); R3.2's strikes = walkScenario round prices ($1,572.95 / $4,212.04); R3.5's frozen count 19 = grader-run topUpQuantityForValue; R3.6's 644 = 75% of 858 at month 36; R3.8's rows reconcile against grader-run capitalRollup to display rounding ($1.53M / $7.67M / $10.2M); R3.9's $157K = 100×$1,572.95; the v1 anchors held in the grader's own engine import (bridge 57,217 @ $1,572.95 · A 75,359 · B 89,380 · C 118,707 · TGE FDV $600M). Two nuances documented, neither a criterion miss: the count NumIn on an imported value-denominated grant shows the raw explicit quantity (0) while the derived count renders in the Instruments summary + runbook (R3.1); the runbook orders its informational cash-free-election note after the s431/deed action items, all stations simultaneously visible (R3.9). CONSOLE-ERROR REPORT for G3 (R3.19): ZERO console errors and zero pageerrors across all 16 probe runs including the two virgin contexts. The Linear MCP was used live (COM-169 fetched to resolve the runbook-ordering ambiguity against the issue's own acceptance text). Repo untouched — all scripts, state dumps, and screenshots live in /tmp/claude/pw/G2010/.
- **G3**: G3 panel-010 graded R3.11–R3.19 as live end-to-end probes against the hash-verified preview at http://[::1]:4173 (served index-P_vMgIRH.js matches scaffold/dist/index.html at start and end of grading; no rebuild). All nine criteria PASS on hard probe evidence — 106 individual assertions across 9 scripts in /tmp/claude/pw/G3010/ (r311–r319.mjs), every flow ending with zero console errors and a byte-identical localStorage restore. Highlights: the COM-174 signed-bind refusal and full pipeline persistence (R3.11); gated-capital crystallisation moving Kerim's base $5.11M→$6.13M exactly per the +10%/$1M uplift with the currentStage re-pricing lens advancing (R3.12); frozen-figure version diffs +$2.10M/v1 vs +$0/v2 with a working print path (R3.13); a 9/9-answered Ispahani artefact persisting on Governance (R3.14); a Carl-scoped band-breach fire→clear cycle (R3.15); the per-scenario pre-TGE toggle ($5.40M→$2.17M token restatement) plus the cash-floor trade reducing value by exactly the configured 2× rate and firing the affordability warning (R3.16); one valuation rendering identically as $2,345.00 across seven enumerated surfaces with no variant forms (R3.17); and grant/consent(+Undo toast+why)/valuation events all landing on the append-only audit log (R3.18). Two early probe failures were my own harness bugs (addInitScript re-seeding on navigation; an at-the-money grant being money-invisible), both diagnosed and corrected — the app behaved correctly throughout. No repo files were written; all artifacts live under /tmp/claude/pw/G3010/.
- **G4**: Panel 010, grader G4 — Wave-7 FULL-rubric pass #3 (post-FIX-9/PR #135), sections R4 (7 criteria) + R6 (3 criteria), graded adversarially against the shared preview at http://[::1]:4173, hash-verified serving index-P_vMgIRH.js (= dist at frosty tip 293bcde) at session start and end; no rebuild performed per the shared-preview instruction. Result: 10 PASS, 0 FAIL, 0 BLOCKED-EXTERNAL — no R4/R6 criterion depends on Wave 6. Both of panel 009's G4 FAILs are independently verified fixed: R6.1 total dist JS shrank 1,037,923 → 969,733 B (matches FIX-9 to the byte; my own cold-/overview payload measure 205.4 kB gzip vs FIX-9's 200.5 kB — gzip-impl delta, both ≤ 290 kB), and the R6.3 fixture now exists at the pinned path, lazy-loads via ?fixture=25x4, and holds max 25.1 ms interaction-to-DOM on the 25×4 board. axe floor holds (0 critical + 0 contrast-serious on /overview+/board; nested-interactive = the documented deliberate pattern). Two non-failing observations for the orchestrator/builder: (1) three axe-critical button-name hits on /configure Select triggers missing programmatic labels — ESOP at adoption (Configure.vue:283), TGE FDV anchor (:495), Advisor pool sizing (:703); they carry visible value text (not icon-only) and sit outside R4.4's enumerated checks and the pinned axe bar, but adding aria-labels per the existing 'Series A prior' convention is a one-line fix each — a future tightening could extend the axe bar to all routes; (2) /proposition's 3 contrast-serious hits are the 13%-opacity watermark whose text is duplicated in the adjacent full-contrast panel (decorative per WCAG 1.4.3). Probe-side false starts (scenario control is role=radio not button; a hidden file input; the known 150ms focus-ring transition artifact that makes synchronous computed-style reads lie) were diagnosed as grader artifacts and corrected before verdict. Repo untouched — all scripts and screenshots in /tmp/claude/pw/G4010/.
